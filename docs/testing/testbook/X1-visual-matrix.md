# X1 — Visual Matrix (theme × zoom)

Every UI surface rendered across all 4 themes and 5 zoom levels. This is where interface-zoom regressions (BUG-4) surface.

**Cases: 1040.**

---

### TC-VIS-0001 — the launcher — reado-dark @ zoom 0.8
**As a** user, **when I** view the launcher with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0002 — the launcher — reado-dark @ zoom 1.0
**As a** user, **when I** view the launcher with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0003 — the launcher — reado-dark @ zoom 1.25
**As a** user, **when I** view the launcher with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0004 — the launcher — reado-dark @ zoom 1.5
**As a** user, **when I** view the launcher with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0005 — the launcher — reado-dark @ zoom 2.0
**As a** user, **when I** view the launcher with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0006 — the launcher — reado-light @ zoom 0.8
**As a** user, **when I** view the launcher with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0007 — the launcher — reado-light @ zoom 1.0
**As a** user, **when I** view the launcher with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0008 — the launcher — reado-light @ zoom 1.25
**As a** user, **when I** view the launcher with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0009 — the launcher — reado-light @ zoom 1.5
**As a** user, **when I** view the launcher with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0010 — the launcher — reado-light @ zoom 2.0
**As a** user, **when I** view the launcher with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0011 — the launcher — reado-sepia @ zoom 0.8
**As a** user, **when I** view the launcher with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0012 — the launcher — reado-sepia @ zoom 1.0
**As a** user, **when I** view the launcher with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0013 — the launcher — reado-sepia @ zoom 1.25
**As a** user, **when I** view the launcher with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0014 — the launcher — reado-sepia @ zoom 1.5
**As a** user, **when I** view the launcher with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0015 — the launcher — reado-sepia @ zoom 2.0
**As a** user, **when I** view the launcher with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0016 — the launcher — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the launcher with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0017 — the launcher — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the launcher with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0018 — the launcher — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the launcher with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0019 — the launcher — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the launcher with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0020 — the launcher — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the launcher with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0021 — the file tree — reado-dark @ zoom 0.8
**As a** user, **when I** view the file tree with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0022 — the file tree — reado-dark @ zoom 1.0
**As a** user, **when I** view the file tree with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0023 — the file tree — reado-dark @ zoom 1.25
**As a** user, **when I** view the file tree with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0024 — the file tree — reado-dark @ zoom 1.5
**As a** user, **when I** view the file tree with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0025 — the file tree — reado-dark @ zoom 2.0
**As a** user, **when I** view the file tree with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0026 — the file tree — reado-light @ zoom 0.8
**As a** user, **when I** view the file tree with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0027 — the file tree — reado-light @ zoom 1.0
**As a** user, **when I** view the file tree with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0028 — the file tree — reado-light @ zoom 1.25
**As a** user, **when I** view the file tree with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0029 — the file tree — reado-light @ zoom 1.5
**As a** user, **when I** view the file tree with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0030 — the file tree — reado-light @ zoom 2.0
**As a** user, **when I** view the file tree with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0031 — the file tree — reado-sepia @ zoom 0.8
**As a** user, **when I** view the file tree with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0032 — the file tree — reado-sepia @ zoom 1.0
**As a** user, **when I** view the file tree with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0033 — the file tree — reado-sepia @ zoom 1.25
**As a** user, **when I** view the file tree with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0034 — the file tree — reado-sepia @ zoom 1.5
**As a** user, **when I** view the file tree with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0035 — the file tree — reado-sepia @ zoom 2.0
**As a** user, **when I** view the file tree with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0036 — the file tree — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the file tree with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0037 — the file tree — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the file tree with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0038 — the file tree — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the file tree with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0039 — the file tree — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the file tree with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0040 — the file tree — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the file tree with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0041 — the editor — reado-dark @ zoom 0.8
**As a** user, **when I** view the editor with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0042 — the editor — reado-dark @ zoom 1.0
**As a** user, **when I** view the editor with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0043 — the editor — reado-dark @ zoom 1.25
**As a** user, **when I** view the editor with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0044 — the editor — reado-dark @ zoom 1.5
**As a** user, **when I** view the editor with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0045 — the editor — reado-dark @ zoom 2.0
**As a** user, **when I** view the editor with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0046 — the editor — reado-light @ zoom 0.8
**As a** user, **when I** view the editor with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0047 — the editor — reado-light @ zoom 1.0
**As a** user, **when I** view the editor with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0048 — the editor — reado-light @ zoom 1.25
**As a** user, **when I** view the editor with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0049 — the editor — reado-light @ zoom 1.5
**As a** user, **when I** view the editor with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0050 — the editor — reado-light @ zoom 2.0
**As a** user, **when I** view the editor with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0051 — the editor — reado-sepia @ zoom 0.8
**As a** user, **when I** view the editor with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0052 — the editor — reado-sepia @ zoom 1.0
**As a** user, **when I** view the editor with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0053 — the editor — reado-sepia @ zoom 1.25
**As a** user, **when I** view the editor with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0054 — the editor — reado-sepia @ zoom 1.5
**As a** user, **when I** view the editor with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0055 — the editor — reado-sepia @ zoom 2.0
**As a** user, **when I** view the editor with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0056 — the editor — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the editor with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0057 — the editor — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the editor with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0058 — the editor — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the editor with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0059 — the editor — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the editor with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0060 — the editor — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the editor with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0061 — the tab bar — reado-dark @ zoom 0.8
**As a** user, **when I** view the tab bar with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0062 — the tab bar — reado-dark @ zoom 1.0
**As a** user, **when I** view the tab bar with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0063 — the tab bar — reado-dark @ zoom 1.25
**As a** user, **when I** view the tab bar with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0064 — the tab bar — reado-dark @ zoom 1.5
**As a** user, **when I** view the tab bar with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0065 — the tab bar — reado-dark @ zoom 2.0
**As a** user, **when I** view the tab bar with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0066 — the tab bar — reado-light @ zoom 0.8
**As a** user, **when I** view the tab bar with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0067 — the tab bar — reado-light @ zoom 1.0
**As a** user, **when I** view the tab bar with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0068 — the tab bar — reado-light @ zoom 1.25
**As a** user, **when I** view the tab bar with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0069 — the tab bar — reado-light @ zoom 1.5
**As a** user, **when I** view the tab bar with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0070 — the tab bar — reado-light @ zoom 2.0
**As a** user, **when I** view the tab bar with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0071 — the tab bar — reado-sepia @ zoom 0.8
**As a** user, **when I** view the tab bar with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0072 — the tab bar — reado-sepia @ zoom 1.0
**As a** user, **when I** view the tab bar with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0073 — the tab bar — reado-sepia @ zoom 1.25
**As a** user, **when I** view the tab bar with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0074 — the tab bar — reado-sepia @ zoom 1.5
**As a** user, **when I** view the tab bar with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0075 — the tab bar — reado-sepia @ zoom 2.0
**As a** user, **when I** view the tab bar with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0076 — the tab bar — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the tab bar with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0077 — the tab bar — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the tab bar with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0078 — the tab bar — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the tab bar with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0079 — the tab bar — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the tab bar with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0080 — the tab bar — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the tab bar with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0081 — the diff view — reado-dark @ zoom 0.8
**As a** user, **when I** view the diff view with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0082 — the diff view — reado-dark @ zoom 1.0
**As a** user, **when I** view the diff view with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0083 — the diff view — reado-dark @ zoom 1.25
**As a** user, **when I** view the diff view with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0084 — the diff view — reado-dark @ zoom 1.5
**As a** user, **when I** view the diff view with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0085 — the diff view — reado-dark @ zoom 2.0
**As a** user, **when I** view the diff view with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0086 — the diff view — reado-light @ zoom 0.8
**As a** user, **when I** view the diff view with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0087 — the diff view — reado-light @ zoom 1.0
**As a** user, **when I** view the diff view with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0088 — the diff view — reado-light @ zoom 1.25
**As a** user, **when I** view the diff view with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0089 — the diff view — reado-light @ zoom 1.5
**As a** user, **when I** view the diff view with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0090 — the diff view — reado-light @ zoom 2.0
**As a** user, **when I** view the diff view with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0091 — the diff view — reado-sepia @ zoom 0.8
**As a** user, **when I** view the diff view with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0092 — the diff view — reado-sepia @ zoom 1.0
**As a** user, **when I** view the diff view with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0093 — the diff view — reado-sepia @ zoom 1.25
**As a** user, **when I** view the diff view with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0094 — the diff view — reado-sepia @ zoom 1.5
**As a** user, **when I** view the diff view with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0095 — the diff view — reado-sepia @ zoom 2.0
**As a** user, **when I** view the diff view with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0096 — the diff view — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the diff view with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0097 — the diff view — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the diff view with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0098 — the diff view — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the diff view with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0099 — the diff view — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the diff view with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0100 — the diff view — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the diff view with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0101 — the blame gutter — reado-dark @ zoom 0.8
**As a** user, **when I** view the blame gutter with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0102 — the blame gutter — reado-dark @ zoom 1.0
**As a** user, **when I** view the blame gutter with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0103 — the blame gutter — reado-dark @ zoom 1.25
**As a** user, **when I** view the blame gutter with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0104 — the blame gutter — reado-dark @ zoom 1.5
**As a** user, **when I** view the blame gutter with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0105 — the blame gutter — reado-dark @ zoom 2.0
**As a** user, **when I** view the blame gutter with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0106 — the blame gutter — reado-light @ zoom 0.8
**As a** user, **when I** view the blame gutter with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0107 — the blame gutter — reado-light @ zoom 1.0
**As a** user, **when I** view the blame gutter with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0108 — the blame gutter — reado-light @ zoom 1.25
**As a** user, **when I** view the blame gutter with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0109 — the blame gutter — reado-light @ zoom 1.5
**As a** user, **when I** view the blame gutter with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0110 — the blame gutter — reado-light @ zoom 2.0
**As a** user, **when I** view the blame gutter with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0111 — the blame gutter — reado-sepia @ zoom 0.8
**As a** user, **when I** view the blame gutter with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0112 — the blame gutter — reado-sepia @ zoom 1.0
**As a** user, **when I** view the blame gutter with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0113 — the blame gutter — reado-sepia @ zoom 1.25
**As a** user, **when I** view the blame gutter with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0114 — the blame gutter — reado-sepia @ zoom 1.5
**As a** user, **when I** view the blame gutter with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0115 — the blame gutter — reado-sepia @ zoom 2.0
**As a** user, **when I** view the blame gutter with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0116 — the blame gutter — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the blame gutter with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0117 — the blame gutter — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the blame gutter with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0118 — the blame gutter — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the blame gutter with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0119 — the blame gutter — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the blame gutter with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0120 — the blame gutter — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the blame gutter with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0121 — the search panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the search panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0122 — the search panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the search panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0123 — the search panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the search panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0124 — the search panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the search panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0125 — the search panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the search panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0126 — the search panel — reado-light @ zoom 0.8
**As a** user, **when I** view the search panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0127 — the search panel — reado-light @ zoom 1.0
**As a** user, **when I** view the search panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0128 — the search panel — reado-light @ zoom 1.25
**As a** user, **when I** view the search panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0129 — the search panel — reado-light @ zoom 1.5
**As a** user, **when I** view the search panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0130 — the search panel — reado-light @ zoom 2.0
**As a** user, **when I** view the search panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0131 — the search panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the search panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0132 — the search panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the search panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0133 — the search panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the search panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0134 — the search panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the search panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0135 — the search panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the search panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0136 — the search panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the search panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0137 — the search panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the search panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0138 — the search panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the search panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0139 — the search panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the search panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0140 — the search panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the search panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0141 — the comments panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the comments panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0142 — the comments panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the comments panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0143 — the comments panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the comments panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0144 — the comments panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the comments panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0145 — the comments panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the comments panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0146 — the comments panel — reado-light @ zoom 0.8
**As a** user, **when I** view the comments panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0147 — the comments panel — reado-light @ zoom 1.0
**As a** user, **when I** view the comments panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0148 — the comments panel — reado-light @ zoom 1.25
**As a** user, **when I** view the comments panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0149 — the comments panel — reado-light @ zoom 1.5
**As a** user, **when I** view the comments panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0150 — the comments panel — reado-light @ zoom 2.0
**As a** user, **when I** view the comments panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0151 — the comments panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the comments panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0152 — the comments panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the comments panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0153 — the comments panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the comments panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0154 — the comments panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the comments panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0155 — the comments panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the comments panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0156 — the comments panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the comments panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0157 — the comments panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the comments panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0158 — the comments panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the comments panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0159 — the comments panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the comments panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0160 — the comments panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the comments panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0161 — a comment thread — reado-dark @ zoom 0.8
**As a** user, **when I** view a comment thread with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0162 — a comment thread — reado-dark @ zoom 1.0
**As a** user, **when I** view a comment thread with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0163 — a comment thread — reado-dark @ zoom 1.25
**As a** user, **when I** view a comment thread with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0164 — a comment thread — reado-dark @ zoom 1.5
**As a** user, **when I** view a comment thread with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0165 — a comment thread — reado-dark @ zoom 2.0
**As a** user, **when I** view a comment thread with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0166 — a comment thread — reado-light @ zoom 0.8
**As a** user, **when I** view a comment thread with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0167 — a comment thread — reado-light @ zoom 1.0
**As a** user, **when I** view a comment thread with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0168 — a comment thread — reado-light @ zoom 1.25
**As a** user, **when I** view a comment thread with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0169 — a comment thread — reado-light @ zoom 1.5
**As a** user, **when I** view a comment thread with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0170 — a comment thread — reado-light @ zoom 2.0
**As a** user, **when I** view a comment thread with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0171 — a comment thread — reado-sepia @ zoom 0.8
**As a** user, **when I** view a comment thread with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0172 — a comment thread — reado-sepia @ zoom 1.0
**As a** user, **when I** view a comment thread with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0173 — a comment thread — reado-sepia @ zoom 1.25
**As a** user, **when I** view a comment thread with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0174 — a comment thread — reado-sepia @ zoom 1.5
**As a** user, **when I** view a comment thread with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0175 — a comment thread — reado-sepia @ zoom 2.0
**As a** user, **when I** view a comment thread with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0176 — a comment thread — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view a comment thread with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0177 — a comment thread — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view a comment thread with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0178 — a comment thread — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view a comment thread with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0179 — a comment thread — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view a comment thread with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0180 — a comment thread — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view a comment thread with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0181 — the comment composer — reado-dark @ zoom 0.8
**As a** user, **when I** view the comment composer with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0182 — the comment composer — reado-dark @ zoom 1.0
**As a** user, **when I** view the comment composer with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0183 — the comment composer — reado-dark @ zoom 1.25
**As a** user, **when I** view the comment composer with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0184 — the comment composer — reado-dark @ zoom 1.5
**As a** user, **when I** view the comment composer with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0185 — the comment composer — reado-dark @ zoom 2.0
**As a** user, **when I** view the comment composer with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0186 — the comment composer — reado-light @ zoom 0.8
**As a** user, **when I** view the comment composer with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0187 — the comment composer — reado-light @ zoom 1.0
**As a** user, **when I** view the comment composer with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0188 — the comment composer — reado-light @ zoom 1.25
**As a** user, **when I** view the comment composer with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0189 — the comment composer — reado-light @ zoom 1.5
**As a** user, **when I** view the comment composer with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0190 — the comment composer — reado-light @ zoom 2.0
**As a** user, **when I** view the comment composer with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0191 — the comment composer — reado-sepia @ zoom 0.8
**As a** user, **when I** view the comment composer with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0192 — the comment composer — reado-sepia @ zoom 1.0
**As a** user, **when I** view the comment composer with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0193 — the comment composer — reado-sepia @ zoom 1.25
**As a** user, **when I** view the comment composer with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0194 — the comment composer — reado-sepia @ zoom 1.5
**As a** user, **when I** view the comment composer with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0195 — the comment composer — reado-sepia @ zoom 2.0
**As a** user, **when I** view the comment composer with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0196 — the comment composer — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the comment composer with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0197 — the comment composer — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the comment composer with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0198 — the comment composer — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the comment composer with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0199 — the comment composer — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the comment composer with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0200 — the comment composer — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the comment composer with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0201 — the git panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the git panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0202 — the git panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the git panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0203 — the git panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the git panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0204 — the git panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the git panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0205 — the git panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the git panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0206 — the git panel — reado-light @ zoom 0.8
**As a** user, **when I** view the git panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0207 — the git panel — reado-light @ zoom 1.0
**As a** user, **when I** view the git panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0208 — the git panel — reado-light @ zoom 1.25
**As a** user, **when I** view the git panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0209 — the git panel — reado-light @ zoom 1.5
**As a** user, **when I** view the git panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0210 — the git panel — reado-light @ zoom 2.0
**As a** user, **when I** view the git panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0211 — the git panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the git panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0212 — the git panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the git panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0213 — the git panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the git panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0214 — the git panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the git panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0215 — the git panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the git panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0216 — the git panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the git panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0217 — the git panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the git panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0218 — the git panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the git panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0219 — the git panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the git panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0220 — the git panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the git panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0221 — the branch switcher — reado-dark @ zoom 0.8
**As a** user, **when I** view the branch switcher with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0222 — the branch switcher — reado-dark @ zoom 1.0
**As a** user, **when I** view the branch switcher with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0223 — the branch switcher — reado-dark @ zoom 1.25
**As a** user, **when I** view the branch switcher with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0224 — the branch switcher — reado-dark @ zoom 1.5
**As a** user, **when I** view the branch switcher with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0225 — the branch switcher — reado-dark @ zoom 2.0
**As a** user, **when I** view the branch switcher with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0226 — the branch switcher — reado-light @ zoom 0.8
**As a** user, **when I** view the branch switcher with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0227 — the branch switcher — reado-light @ zoom 1.0
**As a** user, **when I** view the branch switcher with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0228 — the branch switcher — reado-light @ zoom 1.25
**As a** user, **when I** view the branch switcher with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0229 — the branch switcher — reado-light @ zoom 1.5
**As a** user, **when I** view the branch switcher with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0230 — the branch switcher — reado-light @ zoom 2.0
**As a** user, **when I** view the branch switcher with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0231 — the branch switcher — reado-sepia @ zoom 0.8
**As a** user, **when I** view the branch switcher with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0232 — the branch switcher — reado-sepia @ zoom 1.0
**As a** user, **when I** view the branch switcher with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0233 — the branch switcher — reado-sepia @ zoom 1.25
**As a** user, **when I** view the branch switcher with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0234 — the branch switcher — reado-sepia @ zoom 1.5
**As a** user, **when I** view the branch switcher with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0235 — the branch switcher — reado-sepia @ zoom 2.0
**As a** user, **when I** view the branch switcher with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0236 — the branch switcher — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the branch switcher with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0237 — the branch switcher — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the branch switcher with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0238 — the branch switcher — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the branch switcher with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0239 — the branch switcher — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the branch switcher with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0240 — the branch switcher — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the branch switcher with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0241 — the guided-review panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the guided-review panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0242 — the guided-review panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the guided-review panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0243 — the guided-review panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the guided-review panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0244 — the guided-review panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the guided-review panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0245 — the guided-review panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the guided-review panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0246 — the guided-review panel — reado-light @ zoom 0.8
**As a** user, **when I** view the guided-review panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0247 — the guided-review panel — reado-light @ zoom 1.0
**As a** user, **when I** view the guided-review panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0248 — the guided-review panel — reado-light @ zoom 1.25
**As a** user, **when I** view the guided-review panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0249 — the guided-review panel — reado-light @ zoom 1.5
**As a** user, **when I** view the guided-review panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0250 — the guided-review panel — reado-light @ zoom 2.0
**As a** user, **when I** view the guided-review panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0251 — the guided-review panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the guided-review panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0252 — the guided-review panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the guided-review panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0253 — the guided-review panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the guided-review panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0254 — the guided-review panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the guided-review panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0255 — the guided-review panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the guided-review panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0256 — the guided-review panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the guided-review panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0257 — the guided-review panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the guided-review panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0258 — the guided-review panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the guided-review panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0259 — the guided-review panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the guided-review panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0260 — the guided-review panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the guided-review panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0261 — the terminal — reado-dark @ zoom 0.8
**As a** user, **when I** view the terminal with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0262 — the terminal — reado-dark @ zoom 1.0
**As a** user, **when I** view the terminal with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0263 — the terminal — reado-dark @ zoom 1.25
**As a** user, **when I** view the terminal with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0264 — the terminal — reado-dark @ zoom 1.5
**As a** user, **when I** view the terminal with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0265 — the terminal — reado-dark @ zoom 2.0
**As a** user, **when I** view the terminal with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0266 — the terminal — reado-light @ zoom 0.8
**As a** user, **when I** view the terminal with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0267 — the terminal — reado-light @ zoom 1.0
**As a** user, **when I** view the terminal with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0268 — the terminal — reado-light @ zoom 1.25
**As a** user, **when I** view the terminal with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0269 — the terminal — reado-light @ zoom 1.5
**As a** user, **when I** view the terminal with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0270 — the terminal — reado-light @ zoom 2.0
**As a** user, **when I** view the terminal with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0271 — the terminal — reado-sepia @ zoom 0.8
**As a** user, **when I** view the terminal with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0272 — the terminal — reado-sepia @ zoom 1.0
**As a** user, **when I** view the terminal with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0273 — the terminal — reado-sepia @ zoom 1.25
**As a** user, **when I** view the terminal with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0274 — the terminal — reado-sepia @ zoom 1.5
**As a** user, **when I** view the terminal with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0275 — the terminal — reado-sepia @ zoom 2.0
**As a** user, **when I** view the terminal with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0276 — the terminal — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the terminal with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0277 — the terminal — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the terminal with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0278 — the terminal — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the terminal with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0279 — the terminal — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the terminal with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0280 — the terminal — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the terminal with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0281 — the outline panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the outline panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0282 — the outline panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the outline panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0283 — the outline panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the outline panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0284 — the outline panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the outline panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0285 — the outline panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the outline panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0286 — the outline panel — reado-light @ zoom 0.8
**As a** user, **when I** view the outline panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0287 — the outline panel — reado-light @ zoom 1.0
**As a** user, **when I** view the outline panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0288 — the outline panel — reado-light @ zoom 1.25
**As a** user, **when I** view the outline panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0289 — the outline panel — reado-light @ zoom 1.5
**As a** user, **when I** view the outline panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0290 — the outline panel — reado-light @ zoom 2.0
**As a** user, **when I** view the outline panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0291 — the outline panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the outline panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0292 — the outline panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the outline panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0293 — the outline panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the outline panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0294 — the outline panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the outline panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0295 — the outline panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the outline panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0296 — the outline panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the outline panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0297 — the outline panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the outline panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0298 — the outline panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the outline panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0299 — the outline panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the outline panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0300 — the outline panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the outline panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0301 — the problems panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the problems panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0302 — the problems panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the problems panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0303 — the problems panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the problems panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0304 — the problems panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the problems panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0305 — the problems panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the problems panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0306 — the problems panel — reado-light @ zoom 0.8
**As a** user, **when I** view the problems panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0307 — the problems panel — reado-light @ zoom 1.0
**As a** user, **when I** view the problems panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0308 — the problems panel — reado-light @ zoom 1.25
**As a** user, **when I** view the problems panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0309 — the problems panel — reado-light @ zoom 1.5
**As a** user, **when I** view the problems panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0310 — the problems panel — reado-light @ zoom 2.0
**As a** user, **when I** view the problems panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0311 — the problems panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the problems panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0312 — the problems panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the problems panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0313 — the problems panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the problems panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0314 — the problems panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the problems panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0315 — the problems panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the problems panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0316 — the problems panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the problems panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0317 — the problems panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the problems panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0318 — the problems panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the problems panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0319 — the problems panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the problems panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0320 — the problems panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the problems panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0321 — the hierarchy panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the hierarchy panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0322 — the hierarchy panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the hierarchy panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0323 — the hierarchy panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the hierarchy panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0324 — the hierarchy panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the hierarchy panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0325 — the hierarchy panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the hierarchy panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0326 — the hierarchy panel — reado-light @ zoom 0.8
**As a** user, **when I** view the hierarchy panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0327 — the hierarchy panel — reado-light @ zoom 1.0
**As a** user, **when I** view the hierarchy panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0328 — the hierarchy panel — reado-light @ zoom 1.25
**As a** user, **when I** view the hierarchy panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0329 — the hierarchy panel — reado-light @ zoom 1.5
**As a** user, **when I** view the hierarchy panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0330 — the hierarchy panel — reado-light @ zoom 2.0
**As a** user, **when I** view the hierarchy panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0331 — the hierarchy panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the hierarchy panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0332 — the hierarchy panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the hierarchy panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0333 — the hierarchy panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the hierarchy panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0334 — the hierarchy panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the hierarchy panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0335 — the hierarchy panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the hierarchy panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0336 — the hierarchy panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the hierarchy panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0337 — the hierarchy panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the hierarchy panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0338 — the hierarchy panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the hierarchy panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0339 — the hierarchy panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the hierarchy panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0340 — the hierarchy panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the hierarchy panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0341 — the command palette — reado-dark @ zoom 0.8
**As a** user, **when I** view the command palette with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0342 — the command palette — reado-dark @ zoom 1.0
**As a** user, **when I** view the command palette with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0343 — the command palette — reado-dark @ zoom 1.25
**As a** user, **when I** view the command palette with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0344 — the command palette — reado-dark @ zoom 1.5
**As a** user, **when I** view the command palette with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0345 — the command palette — reado-dark @ zoom 2.0
**As a** user, **when I** view the command palette with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0346 — the command palette — reado-light @ zoom 0.8
**As a** user, **when I** view the command palette with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0347 — the command palette — reado-light @ zoom 1.0
**As a** user, **when I** view the command palette with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0348 — the command palette — reado-light @ zoom 1.25
**As a** user, **when I** view the command palette with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0349 — the command palette — reado-light @ zoom 1.5
**As a** user, **when I** view the command palette with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0350 — the command palette — reado-light @ zoom 2.0
**As a** user, **when I** view the command palette with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0351 — the command palette — reado-sepia @ zoom 0.8
**As a** user, **when I** view the command palette with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0352 — the command palette — reado-sepia @ zoom 1.0
**As a** user, **when I** view the command palette with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0353 — the command palette — reado-sepia @ zoom 1.25
**As a** user, **when I** view the command palette with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0354 — the command palette — reado-sepia @ zoom 1.5
**As a** user, **when I** view the command palette with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0355 — the command palette — reado-sepia @ zoom 2.0
**As a** user, **when I** view the command palette with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0356 — the command palette — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the command palette with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0357 — the command palette — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the command palette with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0358 — the command palette — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the command palette with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0359 — the command palette — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the command palette with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0360 — the command palette — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the command palette with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0361 — the file finder — reado-dark @ zoom 0.8
**As a** user, **when I** view the file finder with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0362 — the file finder — reado-dark @ zoom 1.0
**As a** user, **when I** view the file finder with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0363 — the file finder — reado-dark @ zoom 1.25
**As a** user, **when I** view the file finder with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0364 — the file finder — reado-dark @ zoom 1.5
**As a** user, **when I** view the file finder with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0365 — the file finder — reado-dark @ zoom 2.0
**As a** user, **when I** view the file finder with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0366 — the file finder — reado-light @ zoom 0.8
**As a** user, **when I** view the file finder with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0367 — the file finder — reado-light @ zoom 1.0
**As a** user, **when I** view the file finder with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0368 — the file finder — reado-light @ zoom 1.25
**As a** user, **when I** view the file finder with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0369 — the file finder — reado-light @ zoom 1.5
**As a** user, **when I** view the file finder with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0370 — the file finder — reado-light @ zoom 2.0
**As a** user, **when I** view the file finder with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0371 — the file finder — reado-sepia @ zoom 0.8
**As a** user, **when I** view the file finder with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0372 — the file finder — reado-sepia @ zoom 1.0
**As a** user, **when I** view the file finder with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0373 — the file finder — reado-sepia @ zoom 1.25
**As a** user, **when I** view the file finder with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0374 — the file finder — reado-sepia @ zoom 1.5
**As a** user, **when I** view the file finder with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0375 — the file finder — reado-sepia @ zoom 2.0
**As a** user, **when I** view the file finder with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0376 — the file finder — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the file finder with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0377 — the file finder — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the file finder with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0378 — the file finder — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the file finder with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0379 — the file finder — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the file finder with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0380 — the file finder — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the file finder with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0381 — the settings dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the settings dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0382 — the settings dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the settings dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0383 — the settings dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the settings dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0384 — the settings dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the settings dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0385 — the settings dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the settings dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0386 — the settings dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the settings dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0387 — the settings dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the settings dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0388 — the settings dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the settings dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0389 — the settings dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the settings dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0390 — the settings dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the settings dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0391 — the settings dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the settings dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0392 — the settings dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the settings dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0393 — the settings dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the settings dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0394 — the settings dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the settings dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0395 — the settings dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the settings dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0396 — the settings dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the settings dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0397 — the settings dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the settings dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0398 — the settings dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the settings dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0399 — the settings dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the settings dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0400 — the settings dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the settings dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0401 — the shortcuts dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the shortcuts dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0402 — the shortcuts dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the shortcuts dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0403 — the shortcuts dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the shortcuts dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0404 — the shortcuts dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the shortcuts dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0405 — the shortcuts dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the shortcuts dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0406 — the shortcuts dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the shortcuts dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0407 — the shortcuts dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the shortcuts dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0408 — the shortcuts dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the shortcuts dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0409 — the shortcuts dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the shortcuts dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0410 — the shortcuts dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the shortcuts dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0411 — the shortcuts dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the shortcuts dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0412 — the shortcuts dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the shortcuts dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0413 — the shortcuts dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the shortcuts dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0414 — the shortcuts dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the shortcuts dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0415 — the shortcuts dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the shortcuts dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0416 — the shortcuts dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the shortcuts dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0417 — the shortcuts dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the shortcuts dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0418 — the shortcuts dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the shortcuts dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0419 — the shortcuts dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the shortcuts dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0420 — the shortcuts dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the shortcuts dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0421 — the anywhere dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the anywhere dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0422 — the anywhere dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the anywhere dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0423 — the anywhere dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the anywhere dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0424 — the anywhere dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the anywhere dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0425 — the anywhere dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the anywhere dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0426 — the anywhere dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the anywhere dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0427 — the anywhere dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the anywhere dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0428 — the anywhere dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the anywhere dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0429 — the anywhere dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the anywhere dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0430 — the anywhere dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the anywhere dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0431 — the anywhere dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the anywhere dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0432 — the anywhere dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the anywhere dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0433 — the anywhere dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the anywhere dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0434 — the anywhere dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the anywhere dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0435 — the anywhere dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the anywhere dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0436 — the anywhere dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the anywhere dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0437 — the anywhere dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the anywhere dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0438 — the anywhere dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the anywhere dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0439 — the anywhere dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the anywhere dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0440 — the anywhere dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the anywhere dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0441 — the onboarding modal — reado-dark @ zoom 0.8
**As a** user, **when I** view the onboarding modal with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0442 — the onboarding modal — reado-dark @ zoom 1.0
**As a** user, **when I** view the onboarding modal with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0443 — the onboarding modal — reado-dark @ zoom 1.25
**As a** user, **when I** view the onboarding modal with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0444 — the onboarding modal — reado-dark @ zoom 1.5
**As a** user, **when I** view the onboarding modal with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0445 — the onboarding modal — reado-dark @ zoom 2.0
**As a** user, **when I** view the onboarding modal with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0446 — the onboarding modal — reado-light @ zoom 0.8
**As a** user, **when I** view the onboarding modal with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0447 — the onboarding modal — reado-light @ zoom 1.0
**As a** user, **when I** view the onboarding modal with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0448 — the onboarding modal — reado-light @ zoom 1.25
**As a** user, **when I** view the onboarding modal with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0449 — the onboarding modal — reado-light @ zoom 1.5
**As a** user, **when I** view the onboarding modal with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0450 — the onboarding modal — reado-light @ zoom 2.0
**As a** user, **when I** view the onboarding modal with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0451 — the onboarding modal — reado-sepia @ zoom 0.8
**As a** user, **when I** view the onboarding modal with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0452 — the onboarding modal — reado-sepia @ zoom 1.0
**As a** user, **when I** view the onboarding modal with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0453 — the onboarding modal — reado-sepia @ zoom 1.25
**As a** user, **when I** view the onboarding modal with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0454 — the onboarding modal — reado-sepia @ zoom 1.5
**As a** user, **when I** view the onboarding modal with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0455 — the onboarding modal — reado-sepia @ zoom 2.0
**As a** user, **when I** view the onboarding modal with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0456 — the onboarding modal — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the onboarding modal with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0457 — the onboarding modal — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the onboarding modal with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0458 — the onboarding modal — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the onboarding modal with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0459 — the onboarding modal — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the onboarding modal with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0460 — the onboarding modal — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the onboarding modal with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0461 — the Q&A modal — reado-dark @ zoom 0.8
**As a** user, **when I** view the Q&A modal with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0462 — the Q&A modal — reado-dark @ zoom 1.0
**As a** user, **when I** view the Q&A modal with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0463 — the Q&A modal — reado-dark @ zoom 1.25
**As a** user, **when I** view the Q&A modal with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0464 — the Q&A modal — reado-dark @ zoom 1.5
**As a** user, **when I** view the Q&A modal with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0465 — the Q&A modal — reado-dark @ zoom 2.0
**As a** user, **when I** view the Q&A modal with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0466 — the Q&A modal — reado-light @ zoom 0.8
**As a** user, **when I** view the Q&A modal with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0467 — the Q&A modal — reado-light @ zoom 1.0
**As a** user, **when I** view the Q&A modal with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0468 — the Q&A modal — reado-light @ zoom 1.25
**As a** user, **when I** view the Q&A modal with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0469 — the Q&A modal — reado-light @ zoom 1.5
**As a** user, **when I** view the Q&A modal with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0470 — the Q&A modal — reado-light @ zoom 2.0
**As a** user, **when I** view the Q&A modal with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0471 — the Q&A modal — reado-sepia @ zoom 0.8
**As a** user, **when I** view the Q&A modal with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0472 — the Q&A modal — reado-sepia @ zoom 1.0
**As a** user, **when I** view the Q&A modal with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0473 — the Q&A modal — reado-sepia @ zoom 1.25
**As a** user, **when I** view the Q&A modal with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0474 — the Q&A modal — reado-sepia @ zoom 1.5
**As a** user, **when I** view the Q&A modal with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0475 — the Q&A modal — reado-sepia @ zoom 2.0
**As a** user, **when I** view the Q&A modal with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0476 — the Q&A modal — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the Q&A modal with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0477 — the Q&A modal — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the Q&A modal with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0478 — the Q&A modal — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the Q&A modal with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0479 — the Q&A modal — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the Q&A modal with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0480 — the Q&A modal — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the Q&A modal with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0481 — the semantic modal — reado-dark @ zoom 0.8
**As a** user, **when I** view the semantic modal with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0482 — the semantic modal — reado-dark @ zoom 1.0
**As a** user, **when I** view the semantic modal with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0483 — the semantic modal — reado-dark @ zoom 1.25
**As a** user, **when I** view the semantic modal with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0484 — the semantic modal — reado-dark @ zoom 1.5
**As a** user, **when I** view the semantic modal with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0485 — the semantic modal — reado-dark @ zoom 2.0
**As a** user, **when I** view the semantic modal with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0486 — the semantic modal — reado-light @ zoom 0.8
**As a** user, **when I** view the semantic modal with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0487 — the semantic modal — reado-light @ zoom 1.0
**As a** user, **when I** view the semantic modal with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0488 — the semantic modal — reado-light @ zoom 1.25
**As a** user, **when I** view the semantic modal with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0489 — the semantic modal — reado-light @ zoom 1.5
**As a** user, **when I** view the semantic modal with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0490 — the semantic modal — reado-light @ zoom 2.0
**As a** user, **when I** view the semantic modal with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0491 — the semantic modal — reado-sepia @ zoom 0.8
**As a** user, **when I** view the semantic modal with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0492 — the semantic modal — reado-sepia @ zoom 1.0
**As a** user, **when I** view the semantic modal with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0493 — the semantic modal — reado-sepia @ zoom 1.25
**As a** user, **when I** view the semantic modal with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0494 — the semantic modal — reado-sepia @ zoom 1.5
**As a** user, **when I** view the semantic modal with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0495 — the semantic modal — reado-sepia @ zoom 2.0
**As a** user, **when I** view the semantic modal with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0496 — the semantic modal — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the semantic modal with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0497 — the semantic modal — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the semantic modal with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0498 — the semantic modal — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the semantic modal with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0499 — the semantic modal — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the semantic modal with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0500 — the semantic modal — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the semantic modal with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0501 — the audit dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the audit dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0502 — the audit dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the audit dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0503 — the audit dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the audit dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0504 — the audit dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the audit dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0505 — the audit dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the audit dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0506 — the audit dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the audit dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0507 — the audit dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the audit dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0508 — the audit dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the audit dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0509 — the audit dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the audit dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0510 — the audit dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the audit dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0511 — the audit dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the audit dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0512 — the audit dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the audit dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0513 — the audit dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the audit dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0514 — the audit dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the audit dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0515 — the audit dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the audit dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0516 — the audit dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the audit dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0517 — the audit dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the audit dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0518 — the audit dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the audit dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0519 — the audit dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the audit dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0520 — the audit dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the audit dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0521 — the status bar — reado-dark @ zoom 0.8
**As a** user, **when I** view the status bar with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0522 — the status bar — reado-dark @ zoom 1.0
**As a** user, **when I** view the status bar with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0523 — the status bar — reado-dark @ zoom 1.25
**As a** user, **when I** view the status bar with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0524 — the status bar — reado-dark @ zoom 1.5
**As a** user, **when I** view the status bar with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0525 — the status bar — reado-dark @ zoom 2.0
**As a** user, **when I** view the status bar with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0526 — the status bar — reado-light @ zoom 0.8
**As a** user, **when I** view the status bar with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0527 — the status bar — reado-light @ zoom 1.0
**As a** user, **when I** view the status bar with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0528 — the status bar — reado-light @ zoom 1.25
**As a** user, **when I** view the status bar with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0529 — the status bar — reado-light @ zoom 1.5
**As a** user, **when I** view the status bar with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0530 — the status bar — reado-light @ zoom 2.0
**As a** user, **when I** view the status bar with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0531 — the status bar — reado-sepia @ zoom 0.8
**As a** user, **when I** view the status bar with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0532 — the status bar — reado-sepia @ zoom 1.0
**As a** user, **when I** view the status bar with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0533 — the status bar — reado-sepia @ zoom 1.25
**As a** user, **when I** view the status bar with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0534 — the status bar — reado-sepia @ zoom 1.5
**As a** user, **when I** view the status bar with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0535 — the status bar — reado-sepia @ zoom 2.0
**As a** user, **when I** view the status bar with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0536 — the status bar — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the status bar with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0537 — the status bar — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the status bar with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0538 — the status bar — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the status bar with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0539 — the status bar — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the status bar with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0540 — the status bar — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the status bar with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0541 — the activity bar — reado-dark @ zoom 0.8
**As a** user, **when I** view the activity bar with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0542 — the activity bar — reado-dark @ zoom 1.0
**As a** user, **when I** view the activity bar with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0543 — the activity bar — reado-dark @ zoom 1.25
**As a** user, **when I** view the activity bar with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0544 — the activity bar — reado-dark @ zoom 1.5
**As a** user, **when I** view the activity bar with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0545 — the activity bar — reado-dark @ zoom 2.0
**As a** user, **when I** view the activity bar with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0546 — the activity bar — reado-light @ zoom 0.8
**As a** user, **when I** view the activity bar with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0547 — the activity bar — reado-light @ zoom 1.0
**As a** user, **when I** view the activity bar with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0548 — the activity bar — reado-light @ zoom 1.25
**As a** user, **when I** view the activity bar with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0549 — the activity bar — reado-light @ zoom 1.5
**As a** user, **when I** view the activity bar with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0550 — the activity bar — reado-light @ zoom 2.0
**As a** user, **when I** view the activity bar with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0551 — the activity bar — reado-sepia @ zoom 0.8
**As a** user, **when I** view the activity bar with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0552 — the activity bar — reado-sepia @ zoom 1.0
**As a** user, **when I** view the activity bar with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0553 — the activity bar — reado-sepia @ zoom 1.25
**As a** user, **when I** view the activity bar with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0554 — the activity bar — reado-sepia @ zoom 1.5
**As a** user, **when I** view the activity bar with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0555 — the activity bar — reado-sepia @ zoom 2.0
**As a** user, **when I** view the activity bar with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0556 — the activity bar — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the activity bar with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0557 — the activity bar — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the activity bar with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0558 — the activity bar — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the activity bar with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0559 — the activity bar — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the activity bar with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0560 — the activity bar — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the activity bar with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0561 — the breadcrumb — reado-dark @ zoom 0.8
**As a** user, **when I** view the breadcrumb with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0562 — the breadcrumb — reado-dark @ zoom 1.0
**As a** user, **when I** view the breadcrumb with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0563 — the breadcrumb — reado-dark @ zoom 1.25
**As a** user, **when I** view the breadcrumb with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0564 — the breadcrumb — reado-dark @ zoom 1.5
**As a** user, **when I** view the breadcrumb with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0565 — the breadcrumb — reado-dark @ zoom 2.0
**As a** user, **when I** view the breadcrumb with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0566 — the breadcrumb — reado-light @ zoom 0.8
**As a** user, **when I** view the breadcrumb with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0567 — the breadcrumb — reado-light @ zoom 1.0
**As a** user, **when I** view the breadcrumb with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0568 — the breadcrumb — reado-light @ zoom 1.25
**As a** user, **when I** view the breadcrumb with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0569 — the breadcrumb — reado-light @ zoom 1.5
**As a** user, **when I** view the breadcrumb with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0570 — the breadcrumb — reado-light @ zoom 2.0
**As a** user, **when I** view the breadcrumb with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0571 — the breadcrumb — reado-sepia @ zoom 0.8
**As a** user, **when I** view the breadcrumb with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0572 — the breadcrumb — reado-sepia @ zoom 1.0
**As a** user, **when I** view the breadcrumb with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0573 — the breadcrumb — reado-sepia @ zoom 1.25
**As a** user, **when I** view the breadcrumb with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0574 — the breadcrumb — reado-sepia @ zoom 1.5
**As a** user, **when I** view the breadcrumb with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0575 — the breadcrumb — reado-sepia @ zoom 2.0
**As a** user, **when I** view the breadcrumb with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0576 — the breadcrumb — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the breadcrumb with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0577 — the breadcrumb — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the breadcrumb with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0578 — the breadcrumb — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the breadcrumb with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0579 — the breadcrumb — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the breadcrumb with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0580 — the breadcrumb — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the breadcrumb with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0581 — the structure ribbon — reado-dark @ zoom 0.8
**As a** user, **when I** view the structure ribbon with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0582 — the structure ribbon — reado-dark @ zoom 1.0
**As a** user, **when I** view the structure ribbon with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0583 — the structure ribbon — reado-dark @ zoom 1.25
**As a** user, **when I** view the structure ribbon with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0584 — the structure ribbon — reado-dark @ zoom 1.5
**As a** user, **when I** view the structure ribbon with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0585 — the structure ribbon — reado-dark @ zoom 2.0
**As a** user, **when I** view the structure ribbon with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0586 — the structure ribbon — reado-light @ zoom 0.8
**As a** user, **when I** view the structure ribbon with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0587 — the structure ribbon — reado-light @ zoom 1.0
**As a** user, **when I** view the structure ribbon with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0588 — the structure ribbon — reado-light @ zoom 1.25
**As a** user, **when I** view the structure ribbon with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0589 — the structure ribbon — reado-light @ zoom 1.5
**As a** user, **when I** view the structure ribbon with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0590 — the structure ribbon — reado-light @ zoom 2.0
**As a** user, **when I** view the structure ribbon with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0591 — the structure ribbon — reado-sepia @ zoom 0.8
**As a** user, **when I** view the structure ribbon with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0592 — the structure ribbon — reado-sepia @ zoom 1.0
**As a** user, **when I** view the structure ribbon with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0593 — the structure ribbon — reado-sepia @ zoom 1.25
**As a** user, **when I** view the structure ribbon with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0594 — the structure ribbon — reado-sepia @ zoom 1.5
**As a** user, **when I** view the structure ribbon with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0595 — the structure ribbon — reado-sepia @ zoom 2.0
**As a** user, **when I** view the structure ribbon with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0596 — the structure ribbon — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the structure ribbon with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0597 — the structure ribbon — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the structure ribbon with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0598 — the structure ribbon — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the structure ribbon with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0599 — the structure ribbon — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the structure ribbon with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0600 — the structure ribbon — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the structure ribbon with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0601 — the title bar — reado-dark @ zoom 0.8
**As a** user, **when I** view the title bar with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0602 — the title bar — reado-dark @ zoom 1.0
**As a** user, **when I** view the title bar with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0603 — the title bar — reado-dark @ zoom 1.25
**As a** user, **when I** view the title bar with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0604 — the title bar — reado-dark @ zoom 1.5
**As a** user, **when I** view the title bar with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0605 — the title bar — reado-dark @ zoom 2.0
**As a** user, **when I** view the title bar with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0606 — the title bar — reado-light @ zoom 0.8
**As a** user, **when I** view the title bar with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0607 — the title bar — reado-light @ zoom 1.0
**As a** user, **when I** view the title bar with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0608 — the title bar — reado-light @ zoom 1.25
**As a** user, **when I** view the title bar with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0609 — the title bar — reado-light @ zoom 1.5
**As a** user, **when I** view the title bar with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0610 — the title bar — reado-light @ zoom 2.0
**As a** user, **when I** view the title bar with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0611 — the title bar — reado-sepia @ zoom 0.8
**As a** user, **when I** view the title bar with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0612 — the title bar — reado-sepia @ zoom 1.0
**As a** user, **when I** view the title bar with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0613 — the title bar — reado-sepia @ zoom 1.25
**As a** user, **when I** view the title bar with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0614 — the title bar — reado-sepia @ zoom 1.5
**As a** user, **when I** view the title bar with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0615 — the title bar — reado-sepia @ zoom 2.0
**As a** user, **when I** view the title bar with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0616 — the title bar — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the title bar with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0617 — the title bar — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the title bar with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0618 — the title bar — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the title bar with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0619 — the title bar — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the title bar with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0620 — the title bar — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the title bar with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0621 — the knowledge graph — reado-dark @ zoom 0.8
**As a** user, **when I** view the knowledge graph with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0622 — the knowledge graph — reado-dark @ zoom 1.0
**As a** user, **when I** view the knowledge graph with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0623 — the knowledge graph — reado-dark @ zoom 1.25
**As a** user, **when I** view the knowledge graph with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0624 — the knowledge graph — reado-dark @ zoom 1.5
**As a** user, **when I** view the knowledge graph with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0625 — the knowledge graph — reado-dark @ zoom 2.0
**As a** user, **when I** view the knowledge graph with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0626 — the knowledge graph — reado-light @ zoom 0.8
**As a** user, **when I** view the knowledge graph with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0627 — the knowledge graph — reado-light @ zoom 1.0
**As a** user, **when I** view the knowledge graph with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0628 — the knowledge graph — reado-light @ zoom 1.25
**As a** user, **when I** view the knowledge graph with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0629 — the knowledge graph — reado-light @ zoom 1.5
**As a** user, **when I** view the knowledge graph with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0630 — the knowledge graph — reado-light @ zoom 2.0
**As a** user, **when I** view the knowledge graph with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0631 — the knowledge graph — reado-sepia @ zoom 0.8
**As a** user, **when I** view the knowledge graph with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0632 — the knowledge graph — reado-sepia @ zoom 1.0
**As a** user, **when I** view the knowledge graph with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0633 — the knowledge graph — reado-sepia @ zoom 1.25
**As a** user, **when I** view the knowledge graph with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0634 — the knowledge graph — reado-sepia @ zoom 1.5
**As a** user, **when I** view the knowledge graph with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0635 — the knowledge graph — reado-sepia @ zoom 2.0
**As a** user, **when I** view the knowledge graph with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0636 — the knowledge graph — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the knowledge graph with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0637 — the knowledge graph — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the knowledge graph with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0638 — the knowledge graph — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the knowledge graph with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0639 — the knowledge graph — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the knowledge graph with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0640 — the knowledge graph — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the knowledge graph with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0641 — the docs view — reado-dark @ zoom 0.8
**As a** user, **when I** view the docs view with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0642 — the docs view — reado-dark @ zoom 1.0
**As a** user, **when I** view the docs view with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0643 — the docs view — reado-dark @ zoom 1.25
**As a** user, **when I** view the docs view with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0644 — the docs view — reado-dark @ zoom 1.5
**As a** user, **when I** view the docs view with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0645 — the docs view — reado-dark @ zoom 2.0
**As a** user, **when I** view the docs view with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0646 — the docs view — reado-light @ zoom 0.8
**As a** user, **when I** view the docs view with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0647 — the docs view — reado-light @ zoom 1.0
**As a** user, **when I** view the docs view with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0648 — the docs view — reado-light @ zoom 1.25
**As a** user, **when I** view the docs view with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0649 — the docs view — reado-light @ zoom 1.5
**As a** user, **when I** view the docs view with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0650 — the docs view — reado-light @ zoom 2.0
**As a** user, **when I** view the docs view with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0651 — the docs view — reado-sepia @ zoom 0.8
**As a** user, **when I** view the docs view with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0652 — the docs view — reado-sepia @ zoom 1.0
**As a** user, **when I** view the docs view with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0653 — the docs view — reado-sepia @ zoom 1.25
**As a** user, **when I** view the docs view with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0654 — the docs view — reado-sepia @ zoom 1.5
**As a** user, **when I** view the docs view with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0655 — the docs view — reado-sepia @ zoom 2.0
**As a** user, **when I** view the docs view with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0656 — the docs view — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the docs view with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0657 — the docs view — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the docs view with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0658 — the docs view — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the docs view with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0659 — the docs view — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the docs view with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0660 — the docs view — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the docs view with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0661 — a context menu — reado-dark @ zoom 0.8
**As a** user, **when I** view a context menu with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0662 — a context menu — reado-dark @ zoom 1.0
**As a** user, **when I** view a context menu with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0663 — a context menu — reado-dark @ zoom 1.25
**As a** user, **when I** view a context menu with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0664 — a context menu — reado-dark @ zoom 1.5
**As a** user, **when I** view a context menu with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0665 — a context menu — reado-dark @ zoom 2.0
**As a** user, **when I** view a context menu with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0666 — a context menu — reado-light @ zoom 0.8
**As a** user, **when I** view a context menu with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0667 — a context menu — reado-light @ zoom 1.0
**As a** user, **when I** view a context menu with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0668 — a context menu — reado-light @ zoom 1.25
**As a** user, **when I** view a context menu with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0669 — a context menu — reado-light @ zoom 1.5
**As a** user, **when I** view a context menu with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0670 — a context menu — reado-light @ zoom 2.0
**As a** user, **when I** view a context menu with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0671 — a context menu — reado-sepia @ zoom 0.8
**As a** user, **when I** view a context menu with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0672 — a context menu — reado-sepia @ zoom 1.0
**As a** user, **when I** view a context menu with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0673 — a context menu — reado-sepia @ zoom 1.25
**As a** user, **when I** view a context menu with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0674 — a context menu — reado-sepia @ zoom 1.5
**As a** user, **when I** view a context menu with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0675 — a context menu — reado-sepia @ zoom 2.0
**As a** user, **when I** view a context menu with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0676 — a context menu — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view a context menu with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0677 — a context menu — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view a context menu with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0678 — a context menu — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view a context menu with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0679 — a context menu — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view a context menu with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0680 — a context menu — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view a context menu with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** FAIL → BUG-4 (overlay overflows at high zoom)

### TC-VIS-0681 — the update prompt — reado-dark @ zoom 0.8
**As a** user, **when I** view the update prompt with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0682 — the update prompt — reado-dark @ zoom 1.0
**As a** user, **when I** view the update prompt with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0683 — the update prompt — reado-dark @ zoom 1.25
**As a** user, **when I** view the update prompt with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0684 — the update prompt — reado-dark @ zoom 1.5
**As a** user, **when I** view the update prompt with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0685 — the update prompt — reado-dark @ zoom 2.0
**As a** user, **when I** view the update prompt with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0686 — the update prompt — reado-light @ zoom 0.8
**As a** user, **when I** view the update prompt with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0687 — the update prompt — reado-light @ zoom 1.0
**As a** user, **when I** view the update prompt with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0688 — the update prompt — reado-light @ zoom 1.25
**As a** user, **when I** view the update prompt with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0689 — the update prompt — reado-light @ zoom 1.5
**As a** user, **when I** view the update prompt with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0690 — the update prompt — reado-light @ zoom 2.0
**As a** user, **when I** view the update prompt with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0691 — the update prompt — reado-sepia @ zoom 0.8
**As a** user, **when I** view the update prompt with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0692 — the update prompt — reado-sepia @ zoom 1.0
**As a** user, **when I** view the update prompt with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0693 — the update prompt — reado-sepia @ zoom 1.25
**As a** user, **when I** view the update prompt with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0694 — the update prompt — reado-sepia @ zoom 1.5
**As a** user, **when I** view the update prompt with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0695 — the update prompt — reado-sepia @ zoom 2.0
**As a** user, **when I** view the update prompt with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0696 — the update prompt — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the update prompt with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0697 — the update prompt — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the update prompt with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0698 — the update prompt — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the update prompt with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0699 — the update prompt — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the update prompt with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0700 — the update prompt — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the update prompt with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0701 — the welcome screen — reado-dark @ zoom 0.8
**As a** user, **when I** view the welcome screen with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0702 — the welcome screen — reado-dark @ zoom 1.0
**As a** user, **when I** view the welcome screen with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0703 — the welcome screen — reado-dark @ zoom 1.25
**As a** user, **when I** view the welcome screen with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0704 — the welcome screen — reado-dark @ zoom 1.5
**As a** user, **when I** view the welcome screen with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0705 — the welcome screen — reado-dark @ zoom 2.0
**As a** user, **when I** view the welcome screen with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0706 — the welcome screen — reado-light @ zoom 0.8
**As a** user, **when I** view the welcome screen with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0707 — the welcome screen — reado-light @ zoom 1.0
**As a** user, **when I** view the welcome screen with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0708 — the welcome screen — reado-light @ zoom 1.25
**As a** user, **when I** view the welcome screen with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0709 — the welcome screen — reado-light @ zoom 1.5
**As a** user, **when I** view the welcome screen with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0710 — the welcome screen — reado-light @ zoom 2.0
**As a** user, **when I** view the welcome screen with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0711 — the welcome screen — reado-sepia @ zoom 0.8
**As a** user, **when I** view the welcome screen with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0712 — the welcome screen — reado-sepia @ zoom 1.0
**As a** user, **when I** view the welcome screen with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0713 — the welcome screen — reado-sepia @ zoom 1.25
**As a** user, **when I** view the welcome screen with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0714 — the welcome screen — reado-sepia @ zoom 1.5
**As a** user, **when I** view the welcome screen with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0715 — the welcome screen — reado-sepia @ zoom 2.0
**As a** user, **when I** view the welcome screen with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0716 — the welcome screen — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the welcome screen with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0717 — the welcome screen — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the welcome screen with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0718 — the welcome screen — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the welcome screen with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0719 — the welcome screen — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the welcome screen with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0720 — the welcome screen — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the welcome screen with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0721 — the orphans panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the orphans panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0722 — the orphans panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the orphans panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0723 — the orphans panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the orphans panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0724 — the orphans panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the orphans panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0725 — the orphans panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the orphans panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0726 — the orphans panel — reado-light @ zoom 0.8
**As a** user, **when I** view the orphans panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0727 — the orphans panel — reado-light @ zoom 1.0
**As a** user, **when I** view the orphans panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0728 — the orphans panel — reado-light @ zoom 1.25
**As a** user, **when I** view the orphans panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0729 — the orphans panel — reado-light @ zoom 1.5
**As a** user, **when I** view the orphans panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0730 — the orphans panel — reado-light @ zoom 2.0
**As a** user, **when I** view the orphans panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0731 — the orphans panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the orphans panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0732 — the orphans panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the orphans panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0733 — the orphans panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the orphans panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0734 — the orphans panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the orphans panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0735 — the orphans panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the orphans panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0736 — the orphans panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the orphans panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0737 — the orphans panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the orphans panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0738 — the orphans panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the orphans panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0739 — the orphans panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the orphans panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0740 — the orphans panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the orphans panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0741 — the bookmarks panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the bookmarks panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0742 — the bookmarks panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the bookmarks panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0743 — the bookmarks panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the bookmarks panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0744 — the bookmarks panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the bookmarks panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0745 — the bookmarks panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the bookmarks panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0746 — the bookmarks panel — reado-light @ zoom 0.8
**As a** user, **when I** view the bookmarks panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0747 — the bookmarks panel — reado-light @ zoom 1.0
**As a** user, **when I** view the bookmarks panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0748 — the bookmarks panel — reado-light @ zoom 1.25
**As a** user, **when I** view the bookmarks panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0749 — the bookmarks panel — reado-light @ zoom 1.5
**As a** user, **when I** view the bookmarks panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0750 — the bookmarks panel — reado-light @ zoom 2.0
**As a** user, **when I** view the bookmarks panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0751 — the bookmarks panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the bookmarks panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0752 — the bookmarks panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the bookmarks panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0753 — the bookmarks panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the bookmarks panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0754 — the bookmarks panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the bookmarks panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0755 — the bookmarks panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the bookmarks panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0756 — the bookmarks panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the bookmarks panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0757 — the bookmarks panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the bookmarks panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0758 — the bookmarks panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the bookmarks panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0759 — the bookmarks panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the bookmarks panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0760 — the bookmarks panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the bookmarks panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0761 — the timeline panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the timeline panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0762 — the timeline panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the timeline panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0763 — the timeline panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the timeline panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0764 — the timeline panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the timeline panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0765 — the timeline panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the timeline panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0766 — the timeline panel — reado-light @ zoom 0.8
**As a** user, **when I** view the timeline panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0767 — the timeline panel — reado-light @ zoom 1.0
**As a** user, **when I** view the timeline panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0768 — the timeline panel — reado-light @ zoom 1.25
**As a** user, **when I** view the timeline panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0769 — the timeline panel — reado-light @ zoom 1.5
**As a** user, **when I** view the timeline panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0770 — the timeline panel — reado-light @ zoom 2.0
**As a** user, **when I** view the timeline panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0771 — the timeline panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the timeline panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0772 — the timeline panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the timeline panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0773 — the timeline panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the timeline panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0774 — the timeline panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the timeline panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0775 — the timeline panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the timeline panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0776 — the timeline panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the timeline panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0777 — the timeline panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the timeline panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0778 — the timeline panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the timeline panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0779 — the timeline panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the timeline panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0780 — the timeline panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the timeline panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0781 — the extensions panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the extensions panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0782 — the extensions panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the extensions panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0783 — the extensions panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the extensions panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0784 — the extensions panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the extensions panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0785 — the extensions panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the extensions panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0786 — the extensions panel — reado-light @ zoom 0.8
**As a** user, **when I** view the extensions panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0787 — the extensions panel — reado-light @ zoom 1.0
**As a** user, **when I** view the extensions panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0788 — the extensions panel — reado-light @ zoom 1.25
**As a** user, **when I** view the extensions panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0789 — the extensions panel — reado-light @ zoom 1.5
**As a** user, **when I** view the extensions panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0790 — the extensions panel — reado-light @ zoom 2.0
**As a** user, **when I** view the extensions panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0791 — the extensions panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the extensions panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0792 — the extensions panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the extensions panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0793 — the extensions panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the extensions panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0794 — the extensions panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the extensions panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0795 — the extensions panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the extensions panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0796 — the extensions panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the extensions panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0797 — the extensions panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the extensions panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0798 — the extensions panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the extensions panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0799 — the extensions panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the extensions panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0800 — the extensions panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the extensions panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0801 — the specs panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the specs panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0802 — the specs panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the specs panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0803 — the specs panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the specs panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0804 — the specs panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the specs panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0805 — the specs panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the specs panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0806 — the specs panel — reado-light @ zoom 0.8
**As a** user, **when I** view the specs panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0807 — the specs panel — reado-light @ zoom 1.0
**As a** user, **when I** view the specs panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0808 — the specs panel — reado-light @ zoom 1.25
**As a** user, **when I** view the specs panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0809 — the specs panel — reado-light @ zoom 1.5
**As a** user, **when I** view the specs panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0810 — the specs panel — reado-light @ zoom 2.0
**As a** user, **when I** view the specs panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0811 — the specs panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the specs panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0812 — the specs panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the specs panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0813 — the specs panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the specs panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0814 — the specs panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the specs panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0815 — the specs panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the specs panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0816 — the specs panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the specs panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0817 — the specs panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the specs panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0818 — the specs panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the specs panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0819 — the specs panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the specs panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0820 — the specs panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the specs panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0821 — the tours panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the tours panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0822 — the tours panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the tours panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0823 — the tours panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the tours panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0824 — the tours panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the tours panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0825 — the tours panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the tours panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0826 — the tours panel — reado-light @ zoom 0.8
**As a** user, **when I** view the tours panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0827 — the tours panel — reado-light @ zoom 1.0
**As a** user, **when I** view the tours panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0828 — the tours panel — reado-light @ zoom 1.25
**As a** user, **when I** view the tours panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0829 — the tours panel — reado-light @ zoom 1.5
**As a** user, **when I** view the tours panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0830 — the tours panel — reado-light @ zoom 2.0
**As a** user, **when I** view the tours panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0831 — the tours panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the tours panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0832 — the tours panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the tours panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0833 — the tours panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the tours panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0834 — the tours panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the tours panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0835 — the tours panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the tours panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0836 — the tours panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the tours panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0837 — the tours panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the tours panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0838 — the tours panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the tours panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0839 — the tours panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the tours panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0840 — the tours panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the tours panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0841 — the pre-review panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the pre-review panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0842 — the pre-review panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the pre-review panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0843 — the pre-review panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the pre-review panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0844 — the pre-review panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the pre-review panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0845 — the pre-review panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the pre-review panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0846 — the pre-review panel — reado-light @ zoom 0.8
**As a** user, **when I** view the pre-review panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0847 — the pre-review panel — reado-light @ zoom 1.0
**As a** user, **when I** view the pre-review panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0848 — the pre-review panel — reado-light @ zoom 1.25
**As a** user, **when I** view the pre-review panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0849 — the pre-review panel — reado-light @ zoom 1.5
**As a** user, **when I** view the pre-review panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0850 — the pre-review panel — reado-light @ zoom 2.0
**As a** user, **when I** view the pre-review panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0851 — the pre-review panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the pre-review panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0852 — the pre-review panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the pre-review panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0853 — the pre-review panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the pre-review panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0854 — the pre-review panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the pre-review panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0855 — the pre-review panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the pre-review panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0856 — the pre-review panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the pre-review panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0857 — the pre-review panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the pre-review panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0858 — the pre-review panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the pre-review panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0859 — the pre-review panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the pre-review panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0860 — the pre-review panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the pre-review panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0861 — the tests panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the tests panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0862 — the tests panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the tests panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0863 — the tests panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the tests panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0864 — the tests panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the tests panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0865 — the tests panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the tests panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0866 — the tests panel — reado-light @ zoom 0.8
**As a** user, **when I** view the tests panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0867 — the tests panel — reado-light @ zoom 1.0
**As a** user, **when I** view the tests panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0868 — the tests panel — reado-light @ zoom 1.25
**As a** user, **when I** view the tests panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0869 — the tests panel — reado-light @ zoom 1.5
**As a** user, **when I** view the tests panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0870 — the tests panel — reado-light @ zoom 2.0
**As a** user, **when I** view the tests panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0871 — the tests panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the tests panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0872 — the tests panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the tests panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0873 — the tests panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the tests panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0874 — the tests panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the tests panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0875 — the tests panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the tests panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0876 — the tests panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the tests panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0877 — the tests panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the tests panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0878 — the tests panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the tests panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0879 — the tests panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the tests panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0880 — the tests panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the tests panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0881 — the QA panel — reado-dark @ zoom 0.8
**As a** user, **when I** view the QA panel with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0882 — the QA panel — reado-dark @ zoom 1.0
**As a** user, **when I** view the QA panel with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0883 — the QA panel — reado-dark @ zoom 1.25
**As a** user, **when I** view the QA panel with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0884 — the QA panel — reado-dark @ zoom 1.5
**As a** user, **when I** view the QA panel with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0885 — the QA panel — reado-dark @ zoom 2.0
**As a** user, **when I** view the QA panel with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0886 — the QA panel — reado-light @ zoom 0.8
**As a** user, **when I** view the QA panel with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0887 — the QA panel — reado-light @ zoom 1.0
**As a** user, **when I** view the QA panel with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0888 — the QA panel — reado-light @ zoom 1.25
**As a** user, **when I** view the QA panel with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0889 — the QA panel — reado-light @ zoom 1.5
**As a** user, **when I** view the QA panel with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0890 — the QA panel — reado-light @ zoom 2.0
**As a** user, **when I** view the QA panel with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0891 — the QA panel — reado-sepia @ zoom 0.8
**As a** user, **when I** view the QA panel with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0892 — the QA panel — reado-sepia @ zoom 1.0
**As a** user, **when I** view the QA panel with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0893 — the QA panel — reado-sepia @ zoom 1.25
**As a** user, **when I** view the QA panel with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0894 — the QA panel — reado-sepia @ zoom 1.5
**As a** user, **when I** view the QA panel with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0895 — the QA panel — reado-sepia @ zoom 2.0
**As a** user, **when I** view the QA panel with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0896 — the QA panel — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the QA panel with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0897 — the QA panel — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the QA panel with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0898 — the QA panel — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the QA panel with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0899 — the QA panel — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the QA panel with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0900 — the QA panel — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the QA panel with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0901 — the send-review dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the send-review dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0902 — the send-review dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the send-review dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0903 — the send-review dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the send-review dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0904 — the send-review dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the send-review dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0905 — the send-review dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the send-review dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0906 — the send-review dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the send-review dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0907 — the send-review dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the send-review dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0908 — the send-review dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the send-review dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0909 — the send-review dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the send-review dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0910 — the send-review dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the send-review dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0911 — the send-review dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the send-review dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0912 — the send-review dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the send-review dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0913 — the send-review dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the send-review dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0914 — the send-review dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the send-review dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0915 — the send-review dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the send-review dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0916 — the send-review dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the send-review dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0917 — the send-review dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the send-review dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0918 — the send-review dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the send-review dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0919 — the send-review dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the send-review dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0920 — the send-review dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the send-review dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0921 — the tree-comment dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the tree-comment dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0922 — the tree-comment dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the tree-comment dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0923 — the tree-comment dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the tree-comment dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0924 — the tree-comment dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the tree-comment dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0925 — the tree-comment dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the tree-comment dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0926 — the tree-comment dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the tree-comment dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0927 — the tree-comment dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the tree-comment dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0928 — the tree-comment dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the tree-comment dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0929 — the tree-comment dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the tree-comment dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0930 — the tree-comment dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the tree-comment dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0931 — the tree-comment dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the tree-comment dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0932 — the tree-comment dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the tree-comment dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0933 — the tree-comment dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the tree-comment dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0934 — the tree-comment dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the tree-comment dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0935 — the tree-comment dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the tree-comment dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0936 — the tree-comment dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the tree-comment dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0937 — the tree-comment dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the tree-comment dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0938 — the tree-comment dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the tree-comment dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0939 — the tree-comment dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the tree-comment dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0940 — the tree-comment dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the tree-comment dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0941 — the prompt dialog — reado-dark @ zoom 0.8
**As a** user, **when I** view the prompt dialog with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0942 — the prompt dialog — reado-dark @ zoom 1.0
**As a** user, **when I** view the prompt dialog with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0943 — the prompt dialog — reado-dark @ zoom 1.25
**As a** user, **when I** view the prompt dialog with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0944 — the prompt dialog — reado-dark @ zoom 1.5
**As a** user, **when I** view the prompt dialog with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0945 — the prompt dialog — reado-dark @ zoom 2.0
**As a** user, **when I** view the prompt dialog with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0946 — the prompt dialog — reado-light @ zoom 0.8
**As a** user, **when I** view the prompt dialog with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0947 — the prompt dialog — reado-light @ zoom 1.0
**As a** user, **when I** view the prompt dialog with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0948 — the prompt dialog — reado-light @ zoom 1.25
**As a** user, **when I** view the prompt dialog with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0949 — the prompt dialog — reado-light @ zoom 1.5
**As a** user, **when I** view the prompt dialog with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0950 — the prompt dialog — reado-light @ zoom 2.0
**As a** user, **when I** view the prompt dialog with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0951 — the prompt dialog — reado-sepia @ zoom 0.8
**As a** user, **when I** view the prompt dialog with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0952 — the prompt dialog — reado-sepia @ zoom 1.0
**As a** user, **when I** view the prompt dialog with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0953 — the prompt dialog — reado-sepia @ zoom 1.25
**As a** user, **when I** view the prompt dialog with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0954 — the prompt dialog — reado-sepia @ zoom 1.5
**As a** user, **when I** view the prompt dialog with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0955 — the prompt dialog — reado-sepia @ zoom 2.0
**As a** user, **when I** view the prompt dialog with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0956 — the prompt dialog — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the prompt dialog with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0957 — the prompt dialog — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the prompt dialog with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0958 — the prompt dialog — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the prompt dialog with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0959 — the prompt dialog — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the prompt dialog with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0960 — the prompt dialog — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the prompt dialog with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0961 — the synopsis modal — reado-dark @ zoom 0.8
**As a** user, **when I** view the synopsis modal with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0962 — the synopsis modal — reado-dark @ zoom 1.0
**As a** user, **when I** view the synopsis modal with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0963 — the synopsis modal — reado-dark @ zoom 1.25
**As a** user, **when I** view the synopsis modal with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0964 — the synopsis modal — reado-dark @ zoom 1.5
**As a** user, **when I** view the synopsis modal with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0965 — the synopsis modal — reado-dark @ zoom 2.0
**As a** user, **when I** view the synopsis modal with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0966 — the synopsis modal — reado-light @ zoom 0.8
**As a** user, **when I** view the synopsis modal with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0967 — the synopsis modal — reado-light @ zoom 1.0
**As a** user, **when I** view the synopsis modal with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0968 — the synopsis modal — reado-light @ zoom 1.25
**As a** user, **when I** view the synopsis modal with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0969 — the synopsis modal — reado-light @ zoom 1.5
**As a** user, **when I** view the synopsis modal with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0970 — the synopsis modal — reado-light @ zoom 2.0
**As a** user, **when I** view the synopsis modal with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0971 — the synopsis modal — reado-sepia @ zoom 0.8
**As a** user, **when I** view the synopsis modal with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0972 — the synopsis modal — reado-sepia @ zoom 1.0
**As a** user, **when I** view the synopsis modal with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0973 — the synopsis modal — reado-sepia @ zoom 1.25
**As a** user, **when I** view the synopsis modal with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0974 — the synopsis modal — reado-sepia @ zoom 1.5
**As a** user, **when I** view the synopsis modal with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0975 — the synopsis modal — reado-sepia @ zoom 2.0
**As a** user, **when I** view the synopsis modal with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0976 — the synopsis modal — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the synopsis modal with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0977 — the synopsis modal — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the synopsis modal with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0978 — the synopsis modal — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the synopsis modal with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0979 — the synopsis modal — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the synopsis modal with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0980 — the synopsis modal — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the synopsis modal with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0981 — the gitignore prompt — reado-dark @ zoom 0.8
**As a** user, **when I** view the gitignore prompt with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0982 — the gitignore prompt — reado-dark @ zoom 1.0
**As a** user, **when I** view the gitignore prompt with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0983 — the gitignore prompt — reado-dark @ zoom 1.25
**As a** user, **when I** view the gitignore prompt with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0984 — the gitignore prompt — reado-dark @ zoom 1.5
**As a** user, **when I** view the gitignore prompt with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0985 — the gitignore prompt — reado-dark @ zoom 2.0
**As a** user, **when I** view the gitignore prompt with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0986 — the gitignore prompt — reado-light @ zoom 0.8
**As a** user, **when I** view the gitignore prompt with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0987 — the gitignore prompt — reado-light @ zoom 1.0
**As a** user, **when I** view the gitignore prompt with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0988 — the gitignore prompt — reado-light @ zoom 1.25
**As a** user, **when I** view the gitignore prompt with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0989 — the gitignore prompt — reado-light @ zoom 1.5
**As a** user, **when I** view the gitignore prompt with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0990 — the gitignore prompt — reado-light @ zoom 2.0
**As a** user, **when I** view the gitignore prompt with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0991 — the gitignore prompt — reado-sepia @ zoom 0.8
**As a** user, **when I** view the gitignore prompt with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0992 — the gitignore prompt — reado-sepia @ zoom 1.0
**As a** user, **when I** view the gitignore prompt with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0993 — the gitignore prompt — reado-sepia @ zoom 1.25
**As a** user, **when I** view the gitignore prompt with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0994 — the gitignore prompt — reado-sepia @ zoom 1.5
**As a** user, **when I** view the gitignore prompt with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0995 — the gitignore prompt — reado-sepia @ zoom 2.0
**As a** user, **when I** view the gitignore prompt with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0996 — the gitignore prompt — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the gitignore prompt with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0997 — the gitignore prompt — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the gitignore prompt with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0998 — the gitignore prompt — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the gitignore prompt with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-0999 — the gitignore prompt — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the gitignore prompt with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1000 — the gitignore prompt — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the gitignore prompt with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1001 — the resolve-loop bar — reado-dark @ zoom 0.8
**As a** user, **when I** view the resolve-loop bar with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1002 — the resolve-loop bar — reado-dark @ zoom 1.0
**As a** user, **when I** view the resolve-loop bar with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1003 — the resolve-loop bar — reado-dark @ zoom 1.25
**As a** user, **when I** view the resolve-loop bar with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1004 — the resolve-loop bar — reado-dark @ zoom 1.5
**As a** user, **when I** view the resolve-loop bar with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1005 — the resolve-loop bar — reado-dark @ zoom 2.0
**As a** user, **when I** view the resolve-loop bar with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1006 — the resolve-loop bar — reado-light @ zoom 0.8
**As a** user, **when I** view the resolve-loop bar with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1007 — the resolve-loop bar — reado-light @ zoom 1.0
**As a** user, **when I** view the resolve-loop bar with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1008 — the resolve-loop bar — reado-light @ zoom 1.25
**As a** user, **when I** view the resolve-loop bar with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1009 — the resolve-loop bar — reado-light @ zoom 1.5
**As a** user, **when I** view the resolve-loop bar with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1010 — the resolve-loop bar — reado-light @ zoom 2.0
**As a** user, **when I** view the resolve-loop bar with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1011 — the resolve-loop bar — reado-sepia @ zoom 0.8
**As a** user, **when I** view the resolve-loop bar with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1012 — the resolve-loop bar — reado-sepia @ zoom 1.0
**As a** user, **when I** view the resolve-loop bar with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1013 — the resolve-loop bar — reado-sepia @ zoom 1.25
**As a** user, **when I** view the resolve-loop bar with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1014 — the resolve-loop bar — reado-sepia @ zoom 1.5
**As a** user, **when I** view the resolve-loop bar with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1015 — the resolve-loop bar — reado-sepia @ zoom 2.0
**As a** user, **when I** view the resolve-loop bar with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1016 — the resolve-loop bar — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the resolve-loop bar with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1017 — the resolve-loop bar — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the resolve-loop bar with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1018 — the resolve-loop bar — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the resolve-loop bar with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1019 — the resolve-loop bar — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the resolve-loop bar with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1020 — the resolve-loop bar — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the resolve-loop bar with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1021 — the image viewer — reado-dark @ zoom 0.8
**As a** user, **when I** view the image viewer with the reado-dark theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1022 — the image viewer — reado-dark @ zoom 1.0
**As a** user, **when I** view the image viewer with the reado-dark theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1023 — the image viewer — reado-dark @ zoom 1.25
**As a** user, **when I** view the image viewer with the reado-dark theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1024 — the image viewer — reado-dark @ zoom 1.5
**As a** user, **when I** view the image viewer with the reado-dark theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1025 — the image viewer — reado-dark @ zoom 2.0
**As a** user, **when I** view the image viewer with the reado-dark theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1026 — the image viewer — reado-light @ zoom 0.8
**As a** user, **when I** view the image viewer with the reado-light theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1027 — the image viewer — reado-light @ zoom 1.0
**As a** user, **when I** view the image viewer with the reado-light theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1028 — the image viewer — reado-light @ zoom 1.25
**As a** user, **when I** view the image viewer with the reado-light theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1029 — the image viewer — reado-light @ zoom 1.5
**As a** user, **when I** view the image viewer with the reado-light theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1030 — the image viewer — reado-light @ zoom 2.0
**As a** user, **when I** view the image viewer with the reado-light theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1031 — the image viewer — reado-sepia @ zoom 0.8
**As a** user, **when I** view the image viewer with the reado-sepia theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1032 — the image viewer — reado-sepia @ zoom 1.0
**As a** user, **when I** view the image viewer with the reado-sepia theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1033 — the image viewer — reado-sepia @ zoom 1.25
**As a** user, **when I** view the image viewer with the reado-sepia theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1034 — the image viewer — reado-sepia @ zoom 1.5
**As a** user, **when I** view the image viewer with the reado-sepia theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1035 — the image viewer — reado-sepia @ zoom 2.0
**As a** user, **when I** view the image viewer with the reado-sepia theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1036 — the image viewer — reado-high-contrast @ zoom 0.8
**As a** user, **when I** view the image viewer with the reado-high-contrast theme at interface zoom 0.8, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1037 — the image viewer — reado-high-contrast @ zoom 1.0
**As a** user, **when I** view the image viewer with the reado-high-contrast theme at interface zoom 1.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1038 — the image viewer — reado-high-contrast @ zoom 1.25
**As a** user, **when I** view the image viewer with the reado-high-contrast theme at interface zoom 1.25, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1039 — the image viewer — reado-high-contrast @ zoom 1.5
**As a** user, **when I** view the image viewer with the reado-high-contrast theme at interface zoom 1.5, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO

### TC-VIS-1040 — the image viewer — reado-high-contrast @ zoom 2.0
**As a** user, **when I** view the image viewer with the reado-high-contrast theme at interface zoom 2.0, **I expect** correct layout, contrast, alignment and no clipping/overflow/blur.
- **Result:** TODO
