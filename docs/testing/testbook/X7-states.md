# X7 — Component States & Data Volumes

Every surface across its data/loading/error/disabled states and at small vs large data volumes.

**Cases: 312.**

---

### TC-STATE-0001 — the launcher: empty / no data
**As a** user, **when I** view the launcher in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0002 — the launcher: loading / pending
**As a** user, **when I** view the launcher in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0003 — the launcher: a single item
**As a** user, **when I** view the launcher in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0004 — the launcher: many items (scroll/virtualize)
**As a** user, **when I** view the launcher in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0005 — the launcher: an error state
**As a** user, **when I** view the launcher in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0006 — the launcher: all controls disabled
**As a** user, **when I** view the launcher in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0007 — the file tree: empty / no data
**As a** user, **when I** view the file tree in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0008 — the file tree: loading / pending
**As a** user, **when I** view the file tree in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0009 — the file tree: a single item
**As a** user, **when I** view the file tree in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0010 — the file tree: many items (scroll/virtualize)
**As a** user, **when I** view the file tree in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0011 — the file tree: an error state
**As a** user, **when I** view the file tree in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0012 — the file tree: all controls disabled
**As a** user, **when I** view the file tree in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0013 — the editor: empty / no data
**As a** user, **when I** view the editor in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0014 — the editor: loading / pending
**As a** user, **when I** view the editor in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0015 — the editor: a single item
**As a** user, **when I** view the editor in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0016 — the editor: many items (scroll/virtualize)
**As a** user, **when I** view the editor in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0017 — the editor: an error state
**As a** user, **when I** view the editor in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0018 — the editor: all controls disabled
**As a** user, **when I** view the editor in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0019 — the tab bar: empty / no data
**As a** user, **when I** view the tab bar in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0020 — the tab bar: loading / pending
**As a** user, **when I** view the tab bar in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0021 — the tab bar: a single item
**As a** user, **when I** view the tab bar in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0022 — the tab bar: many items (scroll/virtualize)
**As a** user, **when I** view the tab bar in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0023 — the tab bar: an error state
**As a** user, **when I** view the tab bar in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0024 — the tab bar: all controls disabled
**As a** user, **when I** view the tab bar in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0025 — the diff view: empty / no data
**As a** user, **when I** view the diff view in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0026 — the diff view: loading / pending
**As a** user, **when I** view the diff view in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0027 — the diff view: a single item
**As a** user, **when I** view the diff view in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0028 — the diff view: many items (scroll/virtualize)
**As a** user, **when I** view the diff view in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0029 — the diff view: an error state
**As a** user, **when I** view the diff view in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0030 — the diff view: all controls disabled
**As a** user, **when I** view the diff view in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0031 — the blame gutter: empty / no data
**As a** user, **when I** view the blame gutter in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0032 — the blame gutter: loading / pending
**As a** user, **when I** view the blame gutter in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0033 — the blame gutter: a single item
**As a** user, **when I** view the blame gutter in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0034 — the blame gutter: many items (scroll/virtualize)
**As a** user, **when I** view the blame gutter in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0035 — the blame gutter: an error state
**As a** user, **when I** view the blame gutter in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0036 — the blame gutter: all controls disabled
**As a** user, **when I** view the blame gutter in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0037 — the search panel: empty / no data
**As a** user, **when I** view the search panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0038 — the search panel: loading / pending
**As a** user, **when I** view the search panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0039 — the search panel: a single item
**As a** user, **when I** view the search panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0040 — the search panel: many items (scroll/virtualize)
**As a** user, **when I** view the search panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0041 — the search panel: an error state
**As a** user, **when I** view the search panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0042 — the search panel: all controls disabled
**As a** user, **when I** view the search panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0043 — the comments panel: empty / no data
**As a** user, **when I** view the comments panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0044 — the comments panel: loading / pending
**As a** user, **when I** view the comments panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0045 — the comments panel: a single item
**As a** user, **when I** view the comments panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0046 — the comments panel: many items (scroll/virtualize)
**As a** user, **when I** view the comments panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0047 — the comments panel: an error state
**As a** user, **when I** view the comments panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0048 — the comments panel: all controls disabled
**As a** user, **when I** view the comments panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0049 — a comment thread: empty / no data
**As a** user, **when I** view a comment thread in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0050 — a comment thread: loading / pending
**As a** user, **when I** view a comment thread in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0051 — a comment thread: a single item
**As a** user, **when I** view a comment thread in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0052 — a comment thread: many items (scroll/virtualize)
**As a** user, **when I** view a comment thread in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0053 — a comment thread: an error state
**As a** user, **when I** view a comment thread in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0054 — a comment thread: all controls disabled
**As a** user, **when I** view a comment thread in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0055 — the comment composer: empty / no data
**As a** user, **when I** view the comment composer in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0056 — the comment composer: loading / pending
**As a** user, **when I** view the comment composer in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0057 — the comment composer: a single item
**As a** user, **when I** view the comment composer in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0058 — the comment composer: many items (scroll/virtualize)
**As a** user, **when I** view the comment composer in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0059 — the comment composer: an error state
**As a** user, **when I** view the comment composer in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0060 — the comment composer: all controls disabled
**As a** user, **when I** view the comment composer in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0061 — the git panel: empty / no data
**As a** user, **when I** view the git panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0062 — the git panel: loading / pending
**As a** user, **when I** view the git panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0063 — the git panel: a single item
**As a** user, **when I** view the git panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0064 — the git panel: many items (scroll/virtualize)
**As a** user, **when I** view the git panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0065 — the git panel: an error state
**As a** user, **when I** view the git panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0066 — the git panel: all controls disabled
**As a** user, **when I** view the git panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0067 — the branch switcher: empty / no data
**As a** user, **when I** view the branch switcher in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0068 — the branch switcher: loading / pending
**As a** user, **when I** view the branch switcher in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0069 — the branch switcher: a single item
**As a** user, **when I** view the branch switcher in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0070 — the branch switcher: many items (scroll/virtualize)
**As a** user, **when I** view the branch switcher in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0071 — the branch switcher: an error state
**As a** user, **when I** view the branch switcher in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0072 — the branch switcher: all controls disabled
**As a** user, **when I** view the branch switcher in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0073 — the guided-review panel: empty / no data
**As a** user, **when I** view the guided-review panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0074 — the guided-review panel: loading / pending
**As a** user, **when I** view the guided-review panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0075 — the guided-review panel: a single item
**As a** user, **when I** view the guided-review panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0076 — the guided-review panel: many items (scroll/virtualize)
**As a** user, **when I** view the guided-review panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0077 — the guided-review panel: an error state
**As a** user, **when I** view the guided-review panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0078 — the guided-review panel: all controls disabled
**As a** user, **when I** view the guided-review panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0079 — the terminal: empty / no data
**As a** user, **when I** view the terminal in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0080 — the terminal: loading / pending
**As a** user, **when I** view the terminal in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0081 — the terminal: a single item
**As a** user, **when I** view the terminal in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0082 — the terminal: many items (scroll/virtualize)
**As a** user, **when I** view the terminal in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0083 — the terminal: an error state
**As a** user, **when I** view the terminal in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0084 — the terminal: all controls disabled
**As a** user, **when I** view the terminal in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0085 — the outline panel: empty / no data
**As a** user, **when I** view the outline panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0086 — the outline panel: loading / pending
**As a** user, **when I** view the outline panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0087 — the outline panel: a single item
**As a** user, **when I** view the outline panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0088 — the outline panel: many items (scroll/virtualize)
**As a** user, **when I** view the outline panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0089 — the outline panel: an error state
**As a** user, **when I** view the outline panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0090 — the outline panel: all controls disabled
**As a** user, **when I** view the outline panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0091 — the problems panel: empty / no data
**As a** user, **when I** view the problems panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0092 — the problems panel: loading / pending
**As a** user, **when I** view the problems panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0093 — the problems panel: a single item
**As a** user, **when I** view the problems panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0094 — the problems panel: many items (scroll/virtualize)
**As a** user, **when I** view the problems panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0095 — the problems panel: an error state
**As a** user, **when I** view the problems panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0096 — the problems panel: all controls disabled
**As a** user, **when I** view the problems panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0097 — the hierarchy panel: empty / no data
**As a** user, **when I** view the hierarchy panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0098 — the hierarchy panel: loading / pending
**As a** user, **when I** view the hierarchy panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0099 — the hierarchy panel: a single item
**As a** user, **when I** view the hierarchy panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0100 — the hierarchy panel: many items (scroll/virtualize)
**As a** user, **when I** view the hierarchy panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0101 — the hierarchy panel: an error state
**As a** user, **when I** view the hierarchy panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0102 — the hierarchy panel: all controls disabled
**As a** user, **when I** view the hierarchy panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0103 — the command palette: empty / no data
**As a** user, **when I** view the command palette in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0104 — the command palette: loading / pending
**As a** user, **when I** view the command palette in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0105 — the command palette: a single item
**As a** user, **when I** view the command palette in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0106 — the command palette: many items (scroll/virtualize)
**As a** user, **when I** view the command palette in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0107 — the command palette: an error state
**As a** user, **when I** view the command palette in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0108 — the command palette: all controls disabled
**As a** user, **when I** view the command palette in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0109 — the file finder: empty / no data
**As a** user, **when I** view the file finder in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0110 — the file finder: loading / pending
**As a** user, **when I** view the file finder in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0111 — the file finder: a single item
**As a** user, **when I** view the file finder in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0112 — the file finder: many items (scroll/virtualize)
**As a** user, **when I** view the file finder in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0113 — the file finder: an error state
**As a** user, **when I** view the file finder in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0114 — the file finder: all controls disabled
**As a** user, **when I** view the file finder in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0115 — the settings dialog: empty / no data
**As a** user, **when I** view the settings dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0116 — the settings dialog: loading / pending
**As a** user, **when I** view the settings dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0117 — the settings dialog: a single item
**As a** user, **when I** view the settings dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0118 — the settings dialog: many items (scroll/virtualize)
**As a** user, **when I** view the settings dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0119 — the settings dialog: an error state
**As a** user, **when I** view the settings dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0120 — the settings dialog: all controls disabled
**As a** user, **when I** view the settings dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0121 — the shortcuts dialog: empty / no data
**As a** user, **when I** view the shortcuts dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0122 — the shortcuts dialog: loading / pending
**As a** user, **when I** view the shortcuts dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0123 — the shortcuts dialog: a single item
**As a** user, **when I** view the shortcuts dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0124 — the shortcuts dialog: many items (scroll/virtualize)
**As a** user, **when I** view the shortcuts dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0125 — the shortcuts dialog: an error state
**As a** user, **when I** view the shortcuts dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0126 — the shortcuts dialog: all controls disabled
**As a** user, **when I** view the shortcuts dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0127 — the anywhere dialog: empty / no data
**As a** user, **when I** view the anywhere dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0128 — the anywhere dialog: loading / pending
**As a** user, **when I** view the anywhere dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0129 — the anywhere dialog: a single item
**As a** user, **when I** view the anywhere dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0130 — the anywhere dialog: many items (scroll/virtualize)
**As a** user, **when I** view the anywhere dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0131 — the anywhere dialog: an error state
**As a** user, **when I** view the anywhere dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0132 — the anywhere dialog: all controls disabled
**As a** user, **when I** view the anywhere dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0133 — the onboarding modal: empty / no data
**As a** user, **when I** view the onboarding modal in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0134 — the onboarding modal: loading / pending
**As a** user, **when I** view the onboarding modal in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0135 — the onboarding modal: a single item
**As a** user, **when I** view the onboarding modal in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0136 — the onboarding modal: many items (scroll/virtualize)
**As a** user, **when I** view the onboarding modal in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0137 — the onboarding modal: an error state
**As a** user, **when I** view the onboarding modal in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0138 — the onboarding modal: all controls disabled
**As a** user, **when I** view the onboarding modal in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0139 — the Q&A modal: empty / no data
**As a** user, **when I** view the Q&A modal in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0140 — the Q&A modal: loading / pending
**As a** user, **when I** view the Q&A modal in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0141 — the Q&A modal: a single item
**As a** user, **when I** view the Q&A modal in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0142 — the Q&A modal: many items (scroll/virtualize)
**As a** user, **when I** view the Q&A modal in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0143 — the Q&A modal: an error state
**As a** user, **when I** view the Q&A modal in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0144 — the Q&A modal: all controls disabled
**As a** user, **when I** view the Q&A modal in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0145 — the semantic modal: empty / no data
**As a** user, **when I** view the semantic modal in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0146 — the semantic modal: loading / pending
**As a** user, **when I** view the semantic modal in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0147 — the semantic modal: a single item
**As a** user, **when I** view the semantic modal in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0148 — the semantic modal: many items (scroll/virtualize)
**As a** user, **when I** view the semantic modal in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0149 — the semantic modal: an error state
**As a** user, **when I** view the semantic modal in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0150 — the semantic modal: all controls disabled
**As a** user, **when I** view the semantic modal in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0151 — the audit dialog: empty / no data
**As a** user, **when I** view the audit dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0152 — the audit dialog: loading / pending
**As a** user, **when I** view the audit dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0153 — the audit dialog: a single item
**As a** user, **when I** view the audit dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0154 — the audit dialog: many items (scroll/virtualize)
**As a** user, **when I** view the audit dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0155 — the audit dialog: an error state
**As a** user, **when I** view the audit dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0156 — the audit dialog: all controls disabled
**As a** user, **when I** view the audit dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0157 — the status bar: empty / no data
**As a** user, **when I** view the status bar in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0158 — the status bar: loading / pending
**As a** user, **when I** view the status bar in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0159 — the status bar: a single item
**As a** user, **when I** view the status bar in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0160 — the status bar: many items (scroll/virtualize)
**As a** user, **when I** view the status bar in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0161 — the status bar: an error state
**As a** user, **when I** view the status bar in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0162 — the status bar: all controls disabled
**As a** user, **when I** view the status bar in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0163 — the activity bar: empty / no data
**As a** user, **when I** view the activity bar in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0164 — the activity bar: loading / pending
**As a** user, **when I** view the activity bar in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0165 — the activity bar: a single item
**As a** user, **when I** view the activity bar in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0166 — the activity bar: many items (scroll/virtualize)
**As a** user, **when I** view the activity bar in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0167 — the activity bar: an error state
**As a** user, **when I** view the activity bar in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0168 — the activity bar: all controls disabled
**As a** user, **when I** view the activity bar in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0169 — the breadcrumb: empty / no data
**As a** user, **when I** view the breadcrumb in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0170 — the breadcrumb: loading / pending
**As a** user, **when I** view the breadcrumb in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0171 — the breadcrumb: a single item
**As a** user, **when I** view the breadcrumb in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0172 — the breadcrumb: many items (scroll/virtualize)
**As a** user, **when I** view the breadcrumb in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0173 — the breadcrumb: an error state
**As a** user, **when I** view the breadcrumb in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0174 — the breadcrumb: all controls disabled
**As a** user, **when I** view the breadcrumb in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0175 — the structure ribbon: empty / no data
**As a** user, **when I** view the structure ribbon in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0176 — the structure ribbon: loading / pending
**As a** user, **when I** view the structure ribbon in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0177 — the structure ribbon: a single item
**As a** user, **when I** view the structure ribbon in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0178 — the structure ribbon: many items (scroll/virtualize)
**As a** user, **when I** view the structure ribbon in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0179 — the structure ribbon: an error state
**As a** user, **when I** view the structure ribbon in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0180 — the structure ribbon: all controls disabled
**As a** user, **when I** view the structure ribbon in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0181 — the title bar: empty / no data
**As a** user, **when I** view the title bar in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0182 — the title bar: loading / pending
**As a** user, **when I** view the title bar in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0183 — the title bar: a single item
**As a** user, **when I** view the title bar in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0184 — the title bar: many items (scroll/virtualize)
**As a** user, **when I** view the title bar in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0185 — the title bar: an error state
**As a** user, **when I** view the title bar in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0186 — the title bar: all controls disabled
**As a** user, **when I** view the title bar in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0187 — the knowledge graph: empty / no data
**As a** user, **when I** view the knowledge graph in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0188 — the knowledge graph: loading / pending
**As a** user, **when I** view the knowledge graph in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0189 — the knowledge graph: a single item
**As a** user, **when I** view the knowledge graph in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0190 — the knowledge graph: many items (scroll/virtualize)
**As a** user, **when I** view the knowledge graph in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0191 — the knowledge graph: an error state
**As a** user, **when I** view the knowledge graph in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0192 — the knowledge graph: all controls disabled
**As a** user, **when I** view the knowledge graph in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0193 — the docs view: empty / no data
**As a** user, **when I** view the docs view in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0194 — the docs view: loading / pending
**As a** user, **when I** view the docs view in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0195 — the docs view: a single item
**As a** user, **when I** view the docs view in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0196 — the docs view: many items (scroll/virtualize)
**As a** user, **when I** view the docs view in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0197 — the docs view: an error state
**As a** user, **when I** view the docs view in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0198 — the docs view: all controls disabled
**As a** user, **when I** view the docs view in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0199 — a context menu: empty / no data
**As a** user, **when I** view a context menu in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0200 — a context menu: loading / pending
**As a** user, **when I** view a context menu in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0201 — a context menu: a single item
**As a** user, **when I** view a context menu in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0202 — a context menu: many items (scroll/virtualize)
**As a** user, **when I** view a context menu in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0203 — a context menu: an error state
**As a** user, **when I** view a context menu in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0204 — a context menu: all controls disabled
**As a** user, **when I** view a context menu in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0205 — the update prompt: empty / no data
**As a** user, **when I** view the update prompt in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0206 — the update prompt: loading / pending
**As a** user, **when I** view the update prompt in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0207 — the update prompt: a single item
**As a** user, **when I** view the update prompt in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0208 — the update prompt: many items (scroll/virtualize)
**As a** user, **when I** view the update prompt in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0209 — the update prompt: an error state
**As a** user, **when I** view the update prompt in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0210 — the update prompt: all controls disabled
**As a** user, **when I** view the update prompt in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0211 — the welcome screen: empty / no data
**As a** user, **when I** view the welcome screen in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0212 — the welcome screen: loading / pending
**As a** user, **when I** view the welcome screen in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0213 — the welcome screen: a single item
**As a** user, **when I** view the welcome screen in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0214 — the welcome screen: many items (scroll/virtualize)
**As a** user, **when I** view the welcome screen in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0215 — the welcome screen: an error state
**As a** user, **when I** view the welcome screen in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0216 — the welcome screen: all controls disabled
**As a** user, **when I** view the welcome screen in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0217 — the orphans panel: empty / no data
**As a** user, **when I** view the orphans panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0218 — the orphans panel: loading / pending
**As a** user, **when I** view the orphans panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0219 — the orphans panel: a single item
**As a** user, **when I** view the orphans panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0220 — the orphans panel: many items (scroll/virtualize)
**As a** user, **when I** view the orphans panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0221 — the orphans panel: an error state
**As a** user, **when I** view the orphans panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0222 — the orphans panel: all controls disabled
**As a** user, **when I** view the orphans panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0223 — the bookmarks panel: empty / no data
**As a** user, **when I** view the bookmarks panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0224 — the bookmarks panel: loading / pending
**As a** user, **when I** view the bookmarks panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0225 — the bookmarks panel: a single item
**As a** user, **when I** view the bookmarks panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0226 — the bookmarks panel: many items (scroll/virtualize)
**As a** user, **when I** view the bookmarks panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0227 — the bookmarks panel: an error state
**As a** user, **when I** view the bookmarks panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0228 — the bookmarks panel: all controls disabled
**As a** user, **when I** view the bookmarks panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0229 — the timeline panel: empty / no data
**As a** user, **when I** view the timeline panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0230 — the timeline panel: loading / pending
**As a** user, **when I** view the timeline panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0231 — the timeline panel: a single item
**As a** user, **when I** view the timeline panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0232 — the timeline panel: many items (scroll/virtualize)
**As a** user, **when I** view the timeline panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0233 — the timeline panel: an error state
**As a** user, **when I** view the timeline panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0234 — the timeline panel: all controls disabled
**As a** user, **when I** view the timeline panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0235 — the extensions panel: empty / no data
**As a** user, **when I** view the extensions panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0236 — the extensions panel: loading / pending
**As a** user, **when I** view the extensions panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0237 — the extensions panel: a single item
**As a** user, **when I** view the extensions panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0238 — the extensions panel: many items (scroll/virtualize)
**As a** user, **when I** view the extensions panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0239 — the extensions panel: an error state
**As a** user, **when I** view the extensions panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0240 — the extensions panel: all controls disabled
**As a** user, **when I** view the extensions panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0241 — the specs panel: empty / no data
**As a** user, **when I** view the specs panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0242 — the specs panel: loading / pending
**As a** user, **when I** view the specs panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0243 — the specs panel: a single item
**As a** user, **when I** view the specs panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0244 — the specs panel: many items (scroll/virtualize)
**As a** user, **when I** view the specs panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0245 — the specs panel: an error state
**As a** user, **when I** view the specs panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0246 — the specs panel: all controls disabled
**As a** user, **when I** view the specs panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0247 — the tours panel: empty / no data
**As a** user, **when I** view the tours panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0248 — the tours panel: loading / pending
**As a** user, **when I** view the tours panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0249 — the tours panel: a single item
**As a** user, **when I** view the tours panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0250 — the tours panel: many items (scroll/virtualize)
**As a** user, **when I** view the tours panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0251 — the tours panel: an error state
**As a** user, **when I** view the tours panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0252 — the tours panel: all controls disabled
**As a** user, **when I** view the tours panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0253 — the pre-review panel: empty / no data
**As a** user, **when I** view the pre-review panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0254 — the pre-review panel: loading / pending
**As a** user, **when I** view the pre-review panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0255 — the pre-review panel: a single item
**As a** user, **when I** view the pre-review panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0256 — the pre-review panel: many items (scroll/virtualize)
**As a** user, **when I** view the pre-review panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0257 — the pre-review panel: an error state
**As a** user, **when I** view the pre-review panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0258 — the pre-review panel: all controls disabled
**As a** user, **when I** view the pre-review panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0259 — the tests panel: empty / no data
**As a** user, **when I** view the tests panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0260 — the tests panel: loading / pending
**As a** user, **when I** view the tests panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0261 — the tests panel: a single item
**As a** user, **when I** view the tests panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0262 — the tests panel: many items (scroll/virtualize)
**As a** user, **when I** view the tests panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0263 — the tests panel: an error state
**As a** user, **when I** view the tests panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0264 — the tests panel: all controls disabled
**As a** user, **when I** view the tests panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0265 — the QA panel: empty / no data
**As a** user, **when I** view the QA panel in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0266 — the QA panel: loading / pending
**As a** user, **when I** view the QA panel in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0267 — the QA panel: a single item
**As a** user, **when I** view the QA panel in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0268 — the QA panel: many items (scroll/virtualize)
**As a** user, **when I** view the QA panel in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0269 — the QA panel: an error state
**As a** user, **when I** view the QA panel in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0270 — the QA panel: all controls disabled
**As a** user, **when I** view the QA panel in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0271 — the send-review dialog: empty / no data
**As a** user, **when I** view the send-review dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0272 — the send-review dialog: loading / pending
**As a** user, **when I** view the send-review dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0273 — the send-review dialog: a single item
**As a** user, **when I** view the send-review dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0274 — the send-review dialog: many items (scroll/virtualize)
**As a** user, **when I** view the send-review dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0275 — the send-review dialog: an error state
**As a** user, **when I** view the send-review dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0276 — the send-review dialog: all controls disabled
**As a** user, **when I** view the send-review dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0277 — the tree-comment dialog: empty / no data
**As a** user, **when I** view the tree-comment dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0278 — the tree-comment dialog: loading / pending
**As a** user, **when I** view the tree-comment dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0279 — the tree-comment dialog: a single item
**As a** user, **when I** view the tree-comment dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0280 — the tree-comment dialog: many items (scroll/virtualize)
**As a** user, **when I** view the tree-comment dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0281 — the tree-comment dialog: an error state
**As a** user, **when I** view the tree-comment dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0282 — the tree-comment dialog: all controls disabled
**As a** user, **when I** view the tree-comment dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0283 — the prompt dialog: empty / no data
**As a** user, **when I** view the prompt dialog in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0284 — the prompt dialog: loading / pending
**As a** user, **when I** view the prompt dialog in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0285 — the prompt dialog: a single item
**As a** user, **when I** view the prompt dialog in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0286 — the prompt dialog: many items (scroll/virtualize)
**As a** user, **when I** view the prompt dialog in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0287 — the prompt dialog: an error state
**As a** user, **when I** view the prompt dialog in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0288 — the prompt dialog: all controls disabled
**As a** user, **when I** view the prompt dialog in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0289 — the synopsis modal: empty / no data
**As a** user, **when I** view the synopsis modal in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0290 — the synopsis modal: loading / pending
**As a** user, **when I** view the synopsis modal in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0291 — the synopsis modal: a single item
**As a** user, **when I** view the synopsis modal in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0292 — the synopsis modal: many items (scroll/virtualize)
**As a** user, **when I** view the synopsis modal in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0293 — the synopsis modal: an error state
**As a** user, **when I** view the synopsis modal in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0294 — the synopsis modal: all controls disabled
**As a** user, **when I** view the synopsis modal in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0295 — the gitignore prompt: empty / no data
**As a** user, **when I** view the gitignore prompt in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0296 — the gitignore prompt: loading / pending
**As a** user, **when I** view the gitignore prompt in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0297 — the gitignore prompt: a single item
**As a** user, **when I** view the gitignore prompt in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0298 — the gitignore prompt: many items (scroll/virtualize)
**As a** user, **when I** view the gitignore prompt in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0299 — the gitignore prompt: an error state
**As a** user, **when I** view the gitignore prompt in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0300 — the gitignore prompt: all controls disabled
**As a** user, **when I** view the gitignore prompt in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0301 — the resolve-loop bar: empty / no data
**As a** user, **when I** view the resolve-loop bar in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0302 — the resolve-loop bar: loading / pending
**As a** user, **when I** view the resolve-loop bar in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0303 — the resolve-loop bar: a single item
**As a** user, **when I** view the resolve-loop bar in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0304 — the resolve-loop bar: many items (scroll/virtualize)
**As a** user, **when I** view the resolve-loop bar in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0305 — the resolve-loop bar: an error state
**As a** user, **when I** view the resolve-loop bar in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0306 — the resolve-loop bar: all controls disabled
**As a** user, **when I** view the resolve-loop bar in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0307 — the image viewer: empty / no data
**As a** user, **when I** view the image viewer in the "empty / no data" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0308 — the image viewer: loading / pending
**As a** user, **when I** view the image viewer in the "loading / pending" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0309 — the image viewer: a single item
**As a** user, **when I** view the image viewer in the "a single item" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0310 — the image viewer: many items (scroll/virtualize)
**As a** user, **when I** view the image viewer in the "many items (scroll/virtualize)" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0311 — the image viewer: an error state
**As a** user, **when I** view the image viewer in the "an error state" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO

### TC-STATE-0312 — the image viewer: all controls disabled
**As a** user, **when I** view the image viewer in the "all controls disabled" state, **I expect** a correct, legible rendering with the right affordances and no layout break.
- **Result:** TODO
