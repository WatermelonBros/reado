# X8 — Performance & Responsiveness

Open latency, scroll smoothness, main-thread blocking and teardown memory for every surface.

**Cases: 260.**

---

### TC-PERF-0001 — the launcher: opens within 100ms
**As a** user on a typical machine, **when I** stress the launcher, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0002 — the launcher: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the launcher, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0003 — the launcher: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the launcher, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0004 — the launcher: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the launcher, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0005 — the launcher: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the launcher, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0006 — the file tree: opens within 100ms
**As a** user on a typical machine, **when I** stress the file tree, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0007 — the file tree: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the file tree, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0008 — the file tree: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the file tree, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0009 — the file tree: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the file tree, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0010 — the file tree: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the file tree, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0011 — the editor: opens within 100ms
**As a** user on a typical machine, **when I** stress the editor, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0012 — the editor: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the editor, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0013 — the editor: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the editor, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0014 — the editor: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the editor, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0015 — the editor: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the editor, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0016 — the tab bar: opens within 100ms
**As a** user on a typical machine, **when I** stress the tab bar, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0017 — the tab bar: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the tab bar, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0018 — the tab bar: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the tab bar, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0019 — the tab bar: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the tab bar, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0020 — the tab bar: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the tab bar, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0021 — the diff view: opens within 100ms
**As a** user on a typical machine, **when I** stress the diff view, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0022 — the diff view: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the diff view, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0023 — the diff view: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the diff view, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0024 — the diff view: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the diff view, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0025 — the diff view: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the diff view, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0026 — the blame gutter: opens within 100ms
**As a** user on a typical machine, **when I** stress the blame gutter, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0027 — the blame gutter: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the blame gutter, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0028 — the blame gutter: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the blame gutter, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0029 — the blame gutter: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the blame gutter, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0030 — the blame gutter: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the blame gutter, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0031 — the search panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the search panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0032 — the search panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the search panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0033 — the search panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the search panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0034 — the search panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the search panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0035 — the search panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the search panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0036 — the comments panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the comments panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0037 — the comments panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the comments panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0038 — the comments panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the comments panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0039 — the comments panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the comments panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0040 — the comments panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the comments panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0041 — a comment thread: opens within 100ms
**As a** user on a typical machine, **when I** stress a comment thread, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0042 — a comment thread: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress a comment thread, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0043 — a comment thread: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress a comment thread, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0044 — a comment thread: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress a comment thread, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0045 — a comment thread: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress a comment thread, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0046 — the comment composer: opens within 100ms
**As a** user on a typical machine, **when I** stress the comment composer, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0047 — the comment composer: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the comment composer, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0048 — the comment composer: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the comment composer, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0049 — the comment composer: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the comment composer, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0050 — the comment composer: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the comment composer, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0051 — the git panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the git panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0052 — the git panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the git panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0053 — the git panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the git panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0054 — the git panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the git panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0055 — the git panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the git panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0056 — the branch switcher: opens within 100ms
**As a** user on a typical machine, **when I** stress the branch switcher, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0057 — the branch switcher: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the branch switcher, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0058 — the branch switcher: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the branch switcher, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0059 — the branch switcher: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the branch switcher, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0060 — the branch switcher: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the branch switcher, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0061 — the guided-review panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the guided-review panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0062 — the guided-review panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the guided-review panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0063 — the guided-review panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the guided-review panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0064 — the guided-review panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the guided-review panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0065 — the guided-review panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the guided-review panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0066 — the terminal: opens within 100ms
**As a** user on a typical machine, **when I** stress the terminal, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0067 — the terminal: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the terminal, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0068 — the terminal: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the terminal, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0069 — the terminal: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the terminal, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0070 — the terminal: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the terminal, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0071 — the outline panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the outline panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0072 — the outline panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the outline panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0073 — the outline panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the outline panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0074 — the outline panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the outline panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0075 — the outline panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the outline panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0076 — the problems panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the problems panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0077 — the problems panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the problems panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0078 — the problems panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the problems panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0079 — the problems panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the problems panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0080 — the problems panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the problems panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0081 — the hierarchy panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the hierarchy panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0082 — the hierarchy panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the hierarchy panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0083 — the hierarchy panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the hierarchy panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0084 — the hierarchy panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the hierarchy panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0085 — the hierarchy panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the hierarchy panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0086 — the command palette: opens within 100ms
**As a** user on a typical machine, **when I** stress the command palette, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0087 — the command palette: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the command palette, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0088 — the command palette: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the command palette, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0089 — the command palette: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the command palette, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0090 — the command palette: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the command palette, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0091 — the file finder: opens within 100ms
**As a** user on a typical machine, **when I** stress the file finder, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0092 — the file finder: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the file finder, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0093 — the file finder: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the file finder, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0094 — the file finder: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the file finder, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0095 — the file finder: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the file finder, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0096 — the settings dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the settings dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0097 — the settings dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the settings dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0098 — the settings dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the settings dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0099 — the settings dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the settings dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0100 — the settings dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the settings dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0101 — the shortcuts dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the shortcuts dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0102 — the shortcuts dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the shortcuts dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0103 — the shortcuts dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the shortcuts dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0104 — the shortcuts dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the shortcuts dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0105 — the shortcuts dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the shortcuts dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0106 — the anywhere dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the anywhere dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0107 — the anywhere dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the anywhere dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0108 — the anywhere dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the anywhere dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0109 — the anywhere dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the anywhere dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0110 — the anywhere dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the anywhere dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0111 — the onboarding modal: opens within 100ms
**As a** user on a typical machine, **when I** stress the onboarding modal, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0112 — the onboarding modal: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the onboarding modal, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0113 — the onboarding modal: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the onboarding modal, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0114 — the onboarding modal: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the onboarding modal, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0115 — the onboarding modal: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the onboarding modal, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0116 — the Q&A modal: opens within 100ms
**As a** user on a typical machine, **when I** stress the Q&A modal, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0117 — the Q&A modal: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the Q&A modal, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0118 — the Q&A modal: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the Q&A modal, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0119 — the Q&A modal: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the Q&A modal, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0120 — the Q&A modal: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the Q&A modal, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0121 — the semantic modal: opens within 100ms
**As a** user on a typical machine, **when I** stress the semantic modal, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0122 — the semantic modal: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the semantic modal, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0123 — the semantic modal: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the semantic modal, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0124 — the semantic modal: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the semantic modal, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0125 — the semantic modal: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the semantic modal, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0126 — the audit dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the audit dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0127 — the audit dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the audit dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0128 — the audit dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the audit dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0129 — the audit dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the audit dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0130 — the audit dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the audit dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0131 — the status bar: opens within 100ms
**As a** user on a typical machine, **when I** stress the status bar, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0132 — the status bar: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the status bar, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0133 — the status bar: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the status bar, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0134 — the status bar: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the status bar, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0135 — the status bar: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the status bar, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0136 — the activity bar: opens within 100ms
**As a** user on a typical machine, **when I** stress the activity bar, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0137 — the activity bar: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the activity bar, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0138 — the activity bar: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the activity bar, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0139 — the activity bar: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the activity bar, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0140 — the activity bar: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the activity bar, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0141 — the breadcrumb: opens within 100ms
**As a** user on a typical machine, **when I** stress the breadcrumb, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0142 — the breadcrumb: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the breadcrumb, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0143 — the breadcrumb: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the breadcrumb, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0144 — the breadcrumb: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the breadcrumb, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0145 — the breadcrumb: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the breadcrumb, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0146 — the structure ribbon: opens within 100ms
**As a** user on a typical machine, **when I** stress the structure ribbon, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0147 — the structure ribbon: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the structure ribbon, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0148 — the structure ribbon: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the structure ribbon, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0149 — the structure ribbon: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the structure ribbon, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0150 — the structure ribbon: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the structure ribbon, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0151 — the title bar: opens within 100ms
**As a** user on a typical machine, **when I** stress the title bar, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0152 — the title bar: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the title bar, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0153 — the title bar: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the title bar, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0154 — the title bar: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the title bar, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0155 — the title bar: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the title bar, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0156 — the knowledge graph: opens within 100ms
**As a** user on a typical machine, **when I** stress the knowledge graph, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0157 — the knowledge graph: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the knowledge graph, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0158 — the knowledge graph: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the knowledge graph, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0159 — the knowledge graph: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the knowledge graph, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0160 — the knowledge graph: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the knowledge graph, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0161 — the docs view: opens within 100ms
**As a** user on a typical machine, **when I** stress the docs view, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0162 — the docs view: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the docs view, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0163 — the docs view: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the docs view, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0164 — the docs view: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the docs view, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0165 — the docs view: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the docs view, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0166 — a context menu: opens within 100ms
**As a** user on a typical machine, **when I** stress a context menu, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0167 — a context menu: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress a context menu, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0168 — a context menu: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress a context menu, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0169 — a context menu: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress a context menu, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0170 — a context menu: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress a context menu, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0171 — the update prompt: opens within 100ms
**As a** user on a typical machine, **when I** stress the update prompt, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0172 — the update prompt: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the update prompt, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0173 — the update prompt: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the update prompt, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0174 — the update prompt: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the update prompt, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0175 — the update prompt: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the update prompt, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0176 — the welcome screen: opens within 100ms
**As a** user on a typical machine, **when I** stress the welcome screen, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0177 — the welcome screen: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the welcome screen, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0178 — the welcome screen: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the welcome screen, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0179 — the welcome screen: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the welcome screen, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0180 — the welcome screen: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the welcome screen, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0181 — the orphans panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the orphans panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0182 — the orphans panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the orphans panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0183 — the orphans panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the orphans panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0184 — the orphans panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the orphans panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0185 — the orphans panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the orphans panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0186 — the bookmarks panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the bookmarks panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0187 — the bookmarks panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the bookmarks panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0188 — the bookmarks panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the bookmarks panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0189 — the bookmarks panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the bookmarks panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0190 — the bookmarks panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the bookmarks panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0191 — the timeline panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the timeline panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0192 — the timeline panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the timeline panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0193 — the timeline panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the timeline panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0194 — the timeline panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the timeline panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0195 — the timeline panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the timeline panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0196 — the extensions panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the extensions panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0197 — the extensions panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the extensions panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0198 — the extensions panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the extensions panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0199 — the extensions panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the extensions panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0200 — the extensions panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the extensions panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0201 — the specs panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the specs panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0202 — the specs panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the specs panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0203 — the specs panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the specs panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0204 — the specs panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the specs panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0205 — the specs panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the specs panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0206 — the tours panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the tours panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0207 — the tours panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the tours panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0208 — the tours panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the tours panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0209 — the tours panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the tours panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0210 — the tours panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the tours panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0211 — the pre-review panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the pre-review panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0212 — the pre-review panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the pre-review panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0213 — the pre-review panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the pre-review panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0214 — the pre-review panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the pre-review panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0215 — the pre-review panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the pre-review panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0216 — the tests panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the tests panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0217 — the tests panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the tests panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0218 — the tests panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the tests panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0219 — the tests panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the tests panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0220 — the tests panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the tests panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0221 — the QA panel: opens within 100ms
**As a** user on a typical machine, **when I** stress the QA panel, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0222 — the QA panel: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the QA panel, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0223 — the QA panel: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the QA panel, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0224 — the QA panel: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the QA panel, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0225 — the QA panel: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the QA panel, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0226 — the send-review dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the send-review dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0227 — the send-review dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the send-review dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0228 — the send-review dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the send-review dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0229 — the send-review dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the send-review dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0230 — the send-review dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the send-review dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0231 — the tree-comment dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the tree-comment dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0232 — the tree-comment dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the tree-comment dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0233 — the tree-comment dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the tree-comment dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0234 — the tree-comment dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the tree-comment dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0235 — the tree-comment dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the tree-comment dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0236 — the prompt dialog: opens within 100ms
**As a** user on a typical machine, **when I** stress the prompt dialog, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0237 — the prompt dialog: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the prompt dialog, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0238 — the prompt dialog: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the prompt dialog, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0239 — the prompt dialog: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the prompt dialog, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0240 — the prompt dialog: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the prompt dialog, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0241 — the synopsis modal: opens within 100ms
**As a** user on a typical machine, **when I** stress the synopsis modal, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0242 — the synopsis modal: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the synopsis modal, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0243 — the synopsis modal: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the synopsis modal, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0244 — the synopsis modal: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the synopsis modal, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0245 — the synopsis modal: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the synopsis modal, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0246 — the gitignore prompt: opens within 100ms
**As a** user on a typical machine, **when I** stress the gitignore prompt, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0247 — the gitignore prompt: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the gitignore prompt, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0248 — the gitignore prompt: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the gitignore prompt, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0249 — the gitignore prompt: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the gitignore prompt, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0250 — the gitignore prompt: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the gitignore prompt, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0251 — the resolve-loop bar: opens within 100ms
**As a** user on a typical machine, **when I** stress the resolve-loop bar, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0252 — the resolve-loop bar: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the resolve-loop bar, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0253 — the resolve-loop bar: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the resolve-loop bar, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0254 — the resolve-loop bar: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the resolve-loop bar, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0255 — the resolve-loop bar: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the resolve-loop bar, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO

### TC-PERF-0256 — the image viewer: opens within 100ms
**As a** user on a typical machine, **when I** stress the image viewer, **I expect** it opens within 100ms.
- **Result:** TODO

### TC-PERF-0257 — the image viewer: stays at 60fps while scrolling
**As a** user on a typical machine, **when I** stress the image viewer, **I expect** it stays at 60fps while scrolling.
- **Result:** TODO

### TC-PERF-0258 — the image viewer: does not block the UI thread on heavy work
**As a** user on a typical machine, **when I** stress the image viewer, **I expect** it does not block the UI thread on heavy work.
- **Result:** TODO

### TC-PERF-0259 — the image viewer: frees memory when closed/unmounted
**As a** user on a typical machine, **when I** stress the image viewer, **I expect** it frees memory when closed/unmounted.
- **Result:** TODO

### TC-PERF-0260 — the image viewer: handles 10× the typical data without lag
**As a** user on a typical machine, **when I** stress the image viewer, **I expect** it handles 10× the typical data without lag.
- **Result:** TODO
