# X2 — Responsive / Window Sizes

Every surface across representative window sizes.

**Cases: 832.**

---

### TC-RESP-0001 — the launcher — 1280×832 (default), reado-dark
**As a** user, **when I** view the launcher in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0002 — the launcher — 1280×832 (default), reado-light
**As a** user, **when I** view the launcher in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0003 — the launcher — 1280×832 (default), reado-sepia
**As a** user, **when I** view the launcher in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0004 — the launcher — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the launcher in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0005 — the launcher — 1024×640 (small), reado-dark
**As a** user, **when I** view the launcher in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0006 — the launcher — 1024×640 (small), reado-light
**As a** user, **when I** view the launcher in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0007 — the launcher — 1024×640 (small), reado-sepia
**As a** user, **when I** view the launcher in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0008 — the launcher — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the launcher in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0009 — the launcher — 1920×1080 (large), reado-dark
**As a** user, **when I** view the launcher in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0010 — the launcher — 1920×1080 (large), reado-light
**As a** user, **when I** view the launcher in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0011 — the launcher — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the launcher in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0012 — the launcher — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the launcher in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0013 — the launcher — narrow 700px wide, reado-dark
**As a** user, **when I** view the launcher in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0014 — the launcher — narrow 700px wide, reado-light
**As a** user, **when I** view the launcher in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0015 — the launcher — narrow 700px wide, reado-sepia
**As a** user, **when I** view the launcher in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0016 — the launcher — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the launcher in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0017 — the file tree — 1280×832 (default), reado-dark
**As a** user, **when I** view the file tree in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0018 — the file tree — 1280×832 (default), reado-light
**As a** user, **when I** view the file tree in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0019 — the file tree — 1280×832 (default), reado-sepia
**As a** user, **when I** view the file tree in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0020 — the file tree — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the file tree in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0021 — the file tree — 1024×640 (small), reado-dark
**As a** user, **when I** view the file tree in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0022 — the file tree — 1024×640 (small), reado-light
**As a** user, **when I** view the file tree in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0023 — the file tree — 1024×640 (small), reado-sepia
**As a** user, **when I** view the file tree in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0024 — the file tree — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the file tree in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0025 — the file tree — 1920×1080 (large), reado-dark
**As a** user, **when I** view the file tree in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0026 — the file tree — 1920×1080 (large), reado-light
**As a** user, **when I** view the file tree in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0027 — the file tree — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the file tree in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0028 — the file tree — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the file tree in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0029 — the file tree — narrow 700px wide, reado-dark
**As a** user, **when I** view the file tree in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0030 — the file tree — narrow 700px wide, reado-light
**As a** user, **when I** view the file tree in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0031 — the file tree — narrow 700px wide, reado-sepia
**As a** user, **when I** view the file tree in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0032 — the file tree — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the file tree in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0033 — the editor — 1280×832 (default), reado-dark
**As a** user, **when I** view the editor in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0034 — the editor — 1280×832 (default), reado-light
**As a** user, **when I** view the editor in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0035 — the editor — 1280×832 (default), reado-sepia
**As a** user, **when I** view the editor in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0036 — the editor — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the editor in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0037 — the editor — 1024×640 (small), reado-dark
**As a** user, **when I** view the editor in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0038 — the editor — 1024×640 (small), reado-light
**As a** user, **when I** view the editor in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0039 — the editor — 1024×640 (small), reado-sepia
**As a** user, **when I** view the editor in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0040 — the editor — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the editor in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0041 — the editor — 1920×1080 (large), reado-dark
**As a** user, **when I** view the editor in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0042 — the editor — 1920×1080 (large), reado-light
**As a** user, **when I** view the editor in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0043 — the editor — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the editor in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0044 — the editor — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the editor in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0045 — the editor — narrow 700px wide, reado-dark
**As a** user, **when I** view the editor in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0046 — the editor — narrow 700px wide, reado-light
**As a** user, **when I** view the editor in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0047 — the editor — narrow 700px wide, reado-sepia
**As a** user, **when I** view the editor in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0048 — the editor — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the editor in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0049 — the tab bar — 1280×832 (default), reado-dark
**As a** user, **when I** view the tab bar in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0050 — the tab bar — 1280×832 (default), reado-light
**As a** user, **when I** view the tab bar in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0051 — the tab bar — 1280×832 (default), reado-sepia
**As a** user, **when I** view the tab bar in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0052 — the tab bar — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the tab bar in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0053 — the tab bar — 1024×640 (small), reado-dark
**As a** user, **when I** view the tab bar in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0054 — the tab bar — 1024×640 (small), reado-light
**As a** user, **when I** view the tab bar in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0055 — the tab bar — 1024×640 (small), reado-sepia
**As a** user, **when I** view the tab bar in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0056 — the tab bar — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the tab bar in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0057 — the tab bar — 1920×1080 (large), reado-dark
**As a** user, **when I** view the tab bar in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0058 — the tab bar — 1920×1080 (large), reado-light
**As a** user, **when I** view the tab bar in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0059 — the tab bar — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the tab bar in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0060 — the tab bar — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the tab bar in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0061 — the tab bar — narrow 700px wide, reado-dark
**As a** user, **when I** view the tab bar in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0062 — the tab bar — narrow 700px wide, reado-light
**As a** user, **when I** view the tab bar in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0063 — the tab bar — narrow 700px wide, reado-sepia
**As a** user, **when I** view the tab bar in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0064 — the tab bar — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the tab bar in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0065 — the diff view — 1280×832 (default), reado-dark
**As a** user, **when I** view the diff view in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0066 — the diff view — 1280×832 (default), reado-light
**As a** user, **when I** view the diff view in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0067 — the diff view — 1280×832 (default), reado-sepia
**As a** user, **when I** view the diff view in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0068 — the diff view — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the diff view in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0069 — the diff view — 1024×640 (small), reado-dark
**As a** user, **when I** view the diff view in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0070 — the diff view — 1024×640 (small), reado-light
**As a** user, **when I** view the diff view in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0071 — the diff view — 1024×640 (small), reado-sepia
**As a** user, **when I** view the diff view in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0072 — the diff view — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the diff view in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0073 — the diff view — 1920×1080 (large), reado-dark
**As a** user, **when I** view the diff view in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0074 — the diff view — 1920×1080 (large), reado-light
**As a** user, **when I** view the diff view in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0075 — the diff view — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the diff view in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0076 — the diff view — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the diff view in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0077 — the diff view — narrow 700px wide, reado-dark
**As a** user, **when I** view the diff view in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0078 — the diff view — narrow 700px wide, reado-light
**As a** user, **when I** view the diff view in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0079 — the diff view — narrow 700px wide, reado-sepia
**As a** user, **when I** view the diff view in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0080 — the diff view — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the diff view in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0081 — the blame gutter — 1280×832 (default), reado-dark
**As a** user, **when I** view the blame gutter in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0082 — the blame gutter — 1280×832 (default), reado-light
**As a** user, **when I** view the blame gutter in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0083 — the blame gutter — 1280×832 (default), reado-sepia
**As a** user, **when I** view the blame gutter in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0084 — the blame gutter — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the blame gutter in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0085 — the blame gutter — 1024×640 (small), reado-dark
**As a** user, **when I** view the blame gutter in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0086 — the blame gutter — 1024×640 (small), reado-light
**As a** user, **when I** view the blame gutter in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0087 — the blame gutter — 1024×640 (small), reado-sepia
**As a** user, **when I** view the blame gutter in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0088 — the blame gutter — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the blame gutter in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0089 — the blame gutter — 1920×1080 (large), reado-dark
**As a** user, **when I** view the blame gutter in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0090 — the blame gutter — 1920×1080 (large), reado-light
**As a** user, **when I** view the blame gutter in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0091 — the blame gutter — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the blame gutter in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0092 — the blame gutter — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the blame gutter in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0093 — the blame gutter — narrow 700px wide, reado-dark
**As a** user, **when I** view the blame gutter in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0094 — the blame gutter — narrow 700px wide, reado-light
**As a** user, **when I** view the blame gutter in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0095 — the blame gutter — narrow 700px wide, reado-sepia
**As a** user, **when I** view the blame gutter in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0096 — the blame gutter — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the blame gutter in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0097 — the search panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the search panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0098 — the search panel — 1280×832 (default), reado-light
**As a** user, **when I** view the search panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0099 — the search panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the search panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0100 — the search panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the search panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0101 — the search panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the search panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0102 — the search panel — 1024×640 (small), reado-light
**As a** user, **when I** view the search panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0103 — the search panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the search panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0104 — the search panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the search panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0105 — the search panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the search panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0106 — the search panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the search panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0107 — the search panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the search panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0108 — the search panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the search panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0109 — the search panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the search panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0110 — the search panel — narrow 700px wide, reado-light
**As a** user, **when I** view the search panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0111 — the search panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the search panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0112 — the search panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the search panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0113 — the comments panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the comments panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0114 — the comments panel — 1280×832 (default), reado-light
**As a** user, **when I** view the comments panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0115 — the comments panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the comments panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0116 — the comments panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the comments panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0117 — the comments panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the comments panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0118 — the comments panel — 1024×640 (small), reado-light
**As a** user, **when I** view the comments panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0119 — the comments panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the comments panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0120 — the comments panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the comments panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0121 — the comments panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the comments panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0122 — the comments panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the comments panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0123 — the comments panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the comments panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0124 — the comments panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the comments panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0125 — the comments panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the comments panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0126 — the comments panel — narrow 700px wide, reado-light
**As a** user, **when I** view the comments panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0127 — the comments panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the comments panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0128 — the comments panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the comments panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0129 — a comment thread — 1280×832 (default), reado-dark
**As a** user, **when I** view a comment thread in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0130 — a comment thread — 1280×832 (default), reado-light
**As a** user, **when I** view a comment thread in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0131 — a comment thread — 1280×832 (default), reado-sepia
**As a** user, **when I** view a comment thread in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0132 — a comment thread — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view a comment thread in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0133 — a comment thread — 1024×640 (small), reado-dark
**As a** user, **when I** view a comment thread in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0134 — a comment thread — 1024×640 (small), reado-light
**As a** user, **when I** view a comment thread in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0135 — a comment thread — 1024×640 (small), reado-sepia
**As a** user, **when I** view a comment thread in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0136 — a comment thread — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view a comment thread in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0137 — a comment thread — 1920×1080 (large), reado-dark
**As a** user, **when I** view a comment thread in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0138 — a comment thread — 1920×1080 (large), reado-light
**As a** user, **when I** view a comment thread in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0139 — a comment thread — 1920×1080 (large), reado-sepia
**As a** user, **when I** view a comment thread in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0140 — a comment thread — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view a comment thread in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0141 — a comment thread — narrow 700px wide, reado-dark
**As a** user, **when I** view a comment thread in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0142 — a comment thread — narrow 700px wide, reado-light
**As a** user, **when I** view a comment thread in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0143 — a comment thread — narrow 700px wide, reado-sepia
**As a** user, **when I** view a comment thread in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0144 — a comment thread — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view a comment thread in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0145 — the comment composer — 1280×832 (default), reado-dark
**As a** user, **when I** view the comment composer in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0146 — the comment composer — 1280×832 (default), reado-light
**As a** user, **when I** view the comment composer in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0147 — the comment composer — 1280×832 (default), reado-sepia
**As a** user, **when I** view the comment composer in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0148 — the comment composer — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the comment composer in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0149 — the comment composer — 1024×640 (small), reado-dark
**As a** user, **when I** view the comment composer in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0150 — the comment composer — 1024×640 (small), reado-light
**As a** user, **when I** view the comment composer in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0151 — the comment composer — 1024×640 (small), reado-sepia
**As a** user, **when I** view the comment composer in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0152 — the comment composer — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the comment composer in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0153 — the comment composer — 1920×1080 (large), reado-dark
**As a** user, **when I** view the comment composer in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0154 — the comment composer — 1920×1080 (large), reado-light
**As a** user, **when I** view the comment composer in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0155 — the comment composer — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the comment composer in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0156 — the comment composer — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the comment composer in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0157 — the comment composer — narrow 700px wide, reado-dark
**As a** user, **when I** view the comment composer in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0158 — the comment composer — narrow 700px wide, reado-light
**As a** user, **when I** view the comment composer in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0159 — the comment composer — narrow 700px wide, reado-sepia
**As a** user, **when I** view the comment composer in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0160 — the comment composer — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the comment composer in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0161 — the git panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the git panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0162 — the git panel — 1280×832 (default), reado-light
**As a** user, **when I** view the git panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0163 — the git panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the git panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0164 — the git panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the git panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0165 — the git panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the git panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0166 — the git panel — 1024×640 (small), reado-light
**As a** user, **when I** view the git panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0167 — the git panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the git panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0168 — the git panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the git panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0169 — the git panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the git panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0170 — the git panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the git panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0171 — the git panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the git panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0172 — the git panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the git panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0173 — the git panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the git panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0174 — the git panel — narrow 700px wide, reado-light
**As a** user, **when I** view the git panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0175 — the git panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the git panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0176 — the git panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the git panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0177 — the branch switcher — 1280×832 (default), reado-dark
**As a** user, **when I** view the branch switcher in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0178 — the branch switcher — 1280×832 (default), reado-light
**As a** user, **when I** view the branch switcher in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0179 — the branch switcher — 1280×832 (default), reado-sepia
**As a** user, **when I** view the branch switcher in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0180 — the branch switcher — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the branch switcher in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0181 — the branch switcher — 1024×640 (small), reado-dark
**As a** user, **when I** view the branch switcher in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0182 — the branch switcher — 1024×640 (small), reado-light
**As a** user, **when I** view the branch switcher in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0183 — the branch switcher — 1024×640 (small), reado-sepia
**As a** user, **when I** view the branch switcher in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0184 — the branch switcher — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the branch switcher in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0185 — the branch switcher — 1920×1080 (large), reado-dark
**As a** user, **when I** view the branch switcher in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0186 — the branch switcher — 1920×1080 (large), reado-light
**As a** user, **when I** view the branch switcher in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0187 — the branch switcher — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the branch switcher in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0188 — the branch switcher — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the branch switcher in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0189 — the branch switcher — narrow 700px wide, reado-dark
**As a** user, **when I** view the branch switcher in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0190 — the branch switcher — narrow 700px wide, reado-light
**As a** user, **when I** view the branch switcher in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0191 — the branch switcher — narrow 700px wide, reado-sepia
**As a** user, **when I** view the branch switcher in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0192 — the branch switcher — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the branch switcher in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0193 — the guided-review panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the guided-review panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0194 — the guided-review panel — 1280×832 (default), reado-light
**As a** user, **when I** view the guided-review panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0195 — the guided-review panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the guided-review panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0196 — the guided-review panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the guided-review panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0197 — the guided-review panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the guided-review panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0198 — the guided-review panel — 1024×640 (small), reado-light
**As a** user, **when I** view the guided-review panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0199 — the guided-review panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the guided-review panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0200 — the guided-review panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the guided-review panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0201 — the guided-review panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the guided-review panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0202 — the guided-review panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the guided-review panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0203 — the guided-review panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the guided-review panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0204 — the guided-review panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the guided-review panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0205 — the guided-review panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the guided-review panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0206 — the guided-review panel — narrow 700px wide, reado-light
**As a** user, **when I** view the guided-review panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0207 — the guided-review panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the guided-review panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0208 — the guided-review panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the guided-review panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0209 — the terminal — 1280×832 (default), reado-dark
**As a** user, **when I** view the terminal in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0210 — the terminal — 1280×832 (default), reado-light
**As a** user, **when I** view the terminal in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0211 — the terminal — 1280×832 (default), reado-sepia
**As a** user, **when I** view the terminal in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0212 — the terminal — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the terminal in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0213 — the terminal — 1024×640 (small), reado-dark
**As a** user, **when I** view the terminal in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0214 — the terminal — 1024×640 (small), reado-light
**As a** user, **when I** view the terminal in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0215 — the terminal — 1024×640 (small), reado-sepia
**As a** user, **when I** view the terminal in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0216 — the terminal — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the terminal in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0217 — the terminal — 1920×1080 (large), reado-dark
**As a** user, **when I** view the terminal in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0218 — the terminal — 1920×1080 (large), reado-light
**As a** user, **when I** view the terminal in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0219 — the terminal — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the terminal in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0220 — the terminal — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the terminal in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0221 — the terminal — narrow 700px wide, reado-dark
**As a** user, **when I** view the terminal in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0222 — the terminal — narrow 700px wide, reado-light
**As a** user, **when I** view the terminal in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0223 — the terminal — narrow 700px wide, reado-sepia
**As a** user, **when I** view the terminal in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0224 — the terminal — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the terminal in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0225 — the outline panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the outline panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0226 — the outline panel — 1280×832 (default), reado-light
**As a** user, **when I** view the outline panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0227 — the outline panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the outline panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0228 — the outline panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the outline panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0229 — the outline panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the outline panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0230 — the outline panel — 1024×640 (small), reado-light
**As a** user, **when I** view the outline panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0231 — the outline panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the outline panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0232 — the outline panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the outline panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0233 — the outline panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the outline panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0234 — the outline panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the outline panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0235 — the outline panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the outline panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0236 — the outline panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the outline panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0237 — the outline panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the outline panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0238 — the outline panel — narrow 700px wide, reado-light
**As a** user, **when I** view the outline panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0239 — the outline panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the outline panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0240 — the outline panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the outline panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0241 — the problems panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the problems panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0242 — the problems panel — 1280×832 (default), reado-light
**As a** user, **when I** view the problems panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0243 — the problems panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the problems panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0244 — the problems panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the problems panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0245 — the problems panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the problems panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0246 — the problems panel — 1024×640 (small), reado-light
**As a** user, **when I** view the problems panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0247 — the problems panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the problems panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0248 — the problems panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the problems panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0249 — the problems panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the problems panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0250 — the problems panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the problems panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0251 — the problems panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the problems panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0252 — the problems panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the problems panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0253 — the problems panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the problems panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0254 — the problems panel — narrow 700px wide, reado-light
**As a** user, **when I** view the problems panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0255 — the problems panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the problems panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0256 — the problems panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the problems panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0257 — the hierarchy panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the hierarchy panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0258 — the hierarchy panel — 1280×832 (default), reado-light
**As a** user, **when I** view the hierarchy panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0259 — the hierarchy panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the hierarchy panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0260 — the hierarchy panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the hierarchy panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0261 — the hierarchy panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the hierarchy panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0262 — the hierarchy panel — 1024×640 (small), reado-light
**As a** user, **when I** view the hierarchy panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0263 — the hierarchy panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the hierarchy panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0264 — the hierarchy panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the hierarchy panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0265 — the hierarchy panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the hierarchy panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0266 — the hierarchy panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the hierarchy panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0267 — the hierarchy panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the hierarchy panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0268 — the hierarchy panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the hierarchy panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0269 — the hierarchy panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the hierarchy panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0270 — the hierarchy panel — narrow 700px wide, reado-light
**As a** user, **when I** view the hierarchy panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0271 — the hierarchy panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the hierarchy panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0272 — the hierarchy panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the hierarchy panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0273 — the command palette — 1280×832 (default), reado-dark
**As a** user, **when I** view the command palette in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0274 — the command palette — 1280×832 (default), reado-light
**As a** user, **when I** view the command palette in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0275 — the command palette — 1280×832 (default), reado-sepia
**As a** user, **when I** view the command palette in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0276 — the command palette — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the command palette in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0277 — the command palette — 1024×640 (small), reado-dark
**As a** user, **when I** view the command palette in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0278 — the command palette — 1024×640 (small), reado-light
**As a** user, **when I** view the command palette in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0279 — the command palette — 1024×640 (small), reado-sepia
**As a** user, **when I** view the command palette in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0280 — the command palette — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the command palette in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0281 — the command palette — 1920×1080 (large), reado-dark
**As a** user, **when I** view the command palette in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0282 — the command palette — 1920×1080 (large), reado-light
**As a** user, **when I** view the command palette in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0283 — the command palette — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the command palette in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0284 — the command palette — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the command palette in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0285 — the command palette — narrow 700px wide, reado-dark
**As a** user, **when I** view the command palette in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0286 — the command palette — narrow 700px wide, reado-light
**As a** user, **when I** view the command palette in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0287 — the command palette — narrow 700px wide, reado-sepia
**As a** user, **when I** view the command palette in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0288 — the command palette — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the command palette in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0289 — the file finder — 1280×832 (default), reado-dark
**As a** user, **when I** view the file finder in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0290 — the file finder — 1280×832 (default), reado-light
**As a** user, **when I** view the file finder in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0291 — the file finder — 1280×832 (default), reado-sepia
**As a** user, **when I** view the file finder in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0292 — the file finder — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the file finder in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0293 — the file finder — 1024×640 (small), reado-dark
**As a** user, **when I** view the file finder in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0294 — the file finder — 1024×640 (small), reado-light
**As a** user, **when I** view the file finder in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0295 — the file finder — 1024×640 (small), reado-sepia
**As a** user, **when I** view the file finder in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0296 — the file finder — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the file finder in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0297 — the file finder — 1920×1080 (large), reado-dark
**As a** user, **when I** view the file finder in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0298 — the file finder — 1920×1080 (large), reado-light
**As a** user, **when I** view the file finder in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0299 — the file finder — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the file finder in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0300 — the file finder — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the file finder in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0301 — the file finder — narrow 700px wide, reado-dark
**As a** user, **when I** view the file finder in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0302 — the file finder — narrow 700px wide, reado-light
**As a** user, **when I** view the file finder in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0303 — the file finder — narrow 700px wide, reado-sepia
**As a** user, **when I** view the file finder in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0304 — the file finder — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the file finder in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0305 — the settings dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the settings dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0306 — the settings dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the settings dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0307 — the settings dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the settings dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0308 — the settings dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the settings dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0309 — the settings dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the settings dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0310 — the settings dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the settings dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0311 — the settings dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the settings dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0312 — the settings dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the settings dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0313 — the settings dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the settings dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0314 — the settings dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the settings dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0315 — the settings dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the settings dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0316 — the settings dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the settings dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0317 — the settings dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the settings dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0318 — the settings dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the settings dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0319 — the settings dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the settings dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0320 — the settings dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the settings dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0321 — the shortcuts dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the shortcuts dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0322 — the shortcuts dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the shortcuts dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0323 — the shortcuts dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the shortcuts dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0324 — the shortcuts dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the shortcuts dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0325 — the shortcuts dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the shortcuts dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0326 — the shortcuts dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the shortcuts dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0327 — the shortcuts dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the shortcuts dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0328 — the shortcuts dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the shortcuts dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0329 — the shortcuts dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the shortcuts dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0330 — the shortcuts dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the shortcuts dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0331 — the shortcuts dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the shortcuts dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0332 — the shortcuts dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the shortcuts dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0333 — the shortcuts dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the shortcuts dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0334 — the shortcuts dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the shortcuts dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0335 — the shortcuts dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the shortcuts dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0336 — the shortcuts dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the shortcuts dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0337 — the anywhere dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the anywhere dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0338 — the anywhere dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the anywhere dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0339 — the anywhere dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the anywhere dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0340 — the anywhere dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the anywhere dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0341 — the anywhere dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the anywhere dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0342 — the anywhere dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the anywhere dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0343 — the anywhere dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the anywhere dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0344 — the anywhere dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the anywhere dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0345 — the anywhere dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the anywhere dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0346 — the anywhere dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the anywhere dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0347 — the anywhere dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the anywhere dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0348 — the anywhere dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the anywhere dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0349 — the anywhere dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the anywhere dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0350 — the anywhere dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the anywhere dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0351 — the anywhere dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the anywhere dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0352 — the anywhere dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the anywhere dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0353 — the onboarding modal — 1280×832 (default), reado-dark
**As a** user, **when I** view the onboarding modal in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0354 — the onboarding modal — 1280×832 (default), reado-light
**As a** user, **when I** view the onboarding modal in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0355 — the onboarding modal — 1280×832 (default), reado-sepia
**As a** user, **when I** view the onboarding modal in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0356 — the onboarding modal — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the onboarding modal in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0357 — the onboarding modal — 1024×640 (small), reado-dark
**As a** user, **when I** view the onboarding modal in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0358 — the onboarding modal — 1024×640 (small), reado-light
**As a** user, **when I** view the onboarding modal in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0359 — the onboarding modal — 1024×640 (small), reado-sepia
**As a** user, **when I** view the onboarding modal in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0360 — the onboarding modal — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the onboarding modal in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0361 — the onboarding modal — 1920×1080 (large), reado-dark
**As a** user, **when I** view the onboarding modal in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0362 — the onboarding modal — 1920×1080 (large), reado-light
**As a** user, **when I** view the onboarding modal in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0363 — the onboarding modal — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the onboarding modal in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0364 — the onboarding modal — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the onboarding modal in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0365 — the onboarding modal — narrow 700px wide, reado-dark
**As a** user, **when I** view the onboarding modal in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0366 — the onboarding modal — narrow 700px wide, reado-light
**As a** user, **when I** view the onboarding modal in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0367 — the onboarding modal — narrow 700px wide, reado-sepia
**As a** user, **when I** view the onboarding modal in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0368 — the onboarding modal — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the onboarding modal in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0369 — the Q&A modal — 1280×832 (default), reado-dark
**As a** user, **when I** view the Q&A modal in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0370 — the Q&A modal — 1280×832 (default), reado-light
**As a** user, **when I** view the Q&A modal in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0371 — the Q&A modal — 1280×832 (default), reado-sepia
**As a** user, **when I** view the Q&A modal in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0372 — the Q&A modal — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the Q&A modal in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0373 — the Q&A modal — 1024×640 (small), reado-dark
**As a** user, **when I** view the Q&A modal in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0374 — the Q&A modal — 1024×640 (small), reado-light
**As a** user, **when I** view the Q&A modal in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0375 — the Q&A modal — 1024×640 (small), reado-sepia
**As a** user, **when I** view the Q&A modal in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0376 — the Q&A modal — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the Q&A modal in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0377 — the Q&A modal — 1920×1080 (large), reado-dark
**As a** user, **when I** view the Q&A modal in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0378 — the Q&A modal — 1920×1080 (large), reado-light
**As a** user, **when I** view the Q&A modal in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0379 — the Q&A modal — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the Q&A modal in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0380 — the Q&A modal — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the Q&A modal in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0381 — the Q&A modal — narrow 700px wide, reado-dark
**As a** user, **when I** view the Q&A modal in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0382 — the Q&A modal — narrow 700px wide, reado-light
**As a** user, **when I** view the Q&A modal in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0383 — the Q&A modal — narrow 700px wide, reado-sepia
**As a** user, **when I** view the Q&A modal in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0384 — the Q&A modal — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the Q&A modal in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0385 — the semantic modal — 1280×832 (default), reado-dark
**As a** user, **when I** view the semantic modal in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0386 — the semantic modal — 1280×832 (default), reado-light
**As a** user, **when I** view the semantic modal in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0387 — the semantic modal — 1280×832 (default), reado-sepia
**As a** user, **when I** view the semantic modal in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0388 — the semantic modal — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the semantic modal in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0389 — the semantic modal — 1024×640 (small), reado-dark
**As a** user, **when I** view the semantic modal in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0390 — the semantic modal — 1024×640 (small), reado-light
**As a** user, **when I** view the semantic modal in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0391 — the semantic modal — 1024×640 (small), reado-sepia
**As a** user, **when I** view the semantic modal in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0392 — the semantic modal — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the semantic modal in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0393 — the semantic modal — 1920×1080 (large), reado-dark
**As a** user, **when I** view the semantic modal in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0394 — the semantic modal — 1920×1080 (large), reado-light
**As a** user, **when I** view the semantic modal in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0395 — the semantic modal — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the semantic modal in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0396 — the semantic modal — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the semantic modal in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0397 — the semantic modal — narrow 700px wide, reado-dark
**As a** user, **when I** view the semantic modal in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0398 — the semantic modal — narrow 700px wide, reado-light
**As a** user, **when I** view the semantic modal in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0399 — the semantic modal — narrow 700px wide, reado-sepia
**As a** user, **when I** view the semantic modal in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0400 — the semantic modal — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the semantic modal in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0401 — the audit dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the audit dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0402 — the audit dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the audit dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0403 — the audit dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the audit dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0404 — the audit dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the audit dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0405 — the audit dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the audit dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0406 — the audit dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the audit dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0407 — the audit dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the audit dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0408 — the audit dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the audit dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0409 — the audit dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the audit dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0410 — the audit dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the audit dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0411 — the audit dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the audit dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0412 — the audit dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the audit dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0413 — the audit dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the audit dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0414 — the audit dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the audit dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0415 — the audit dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the audit dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0416 — the audit dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the audit dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0417 — the status bar — 1280×832 (default), reado-dark
**As a** user, **when I** view the status bar in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0418 — the status bar — 1280×832 (default), reado-light
**As a** user, **when I** view the status bar in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0419 — the status bar — 1280×832 (default), reado-sepia
**As a** user, **when I** view the status bar in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0420 — the status bar — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the status bar in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0421 — the status bar — 1024×640 (small), reado-dark
**As a** user, **when I** view the status bar in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0422 — the status bar — 1024×640 (small), reado-light
**As a** user, **when I** view the status bar in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0423 — the status bar — 1024×640 (small), reado-sepia
**As a** user, **when I** view the status bar in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0424 — the status bar — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the status bar in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0425 — the status bar — 1920×1080 (large), reado-dark
**As a** user, **when I** view the status bar in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0426 — the status bar — 1920×1080 (large), reado-light
**As a** user, **when I** view the status bar in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0427 — the status bar — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the status bar in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0428 — the status bar — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the status bar in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0429 — the status bar — narrow 700px wide, reado-dark
**As a** user, **when I** view the status bar in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0430 — the status bar — narrow 700px wide, reado-light
**As a** user, **when I** view the status bar in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0431 — the status bar — narrow 700px wide, reado-sepia
**As a** user, **when I** view the status bar in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0432 — the status bar — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the status bar in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0433 — the activity bar — 1280×832 (default), reado-dark
**As a** user, **when I** view the activity bar in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0434 — the activity bar — 1280×832 (default), reado-light
**As a** user, **when I** view the activity bar in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0435 — the activity bar — 1280×832 (default), reado-sepia
**As a** user, **when I** view the activity bar in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0436 — the activity bar — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the activity bar in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0437 — the activity bar — 1024×640 (small), reado-dark
**As a** user, **when I** view the activity bar in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0438 — the activity bar — 1024×640 (small), reado-light
**As a** user, **when I** view the activity bar in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0439 — the activity bar — 1024×640 (small), reado-sepia
**As a** user, **when I** view the activity bar in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0440 — the activity bar — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the activity bar in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0441 — the activity bar — 1920×1080 (large), reado-dark
**As a** user, **when I** view the activity bar in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0442 — the activity bar — 1920×1080 (large), reado-light
**As a** user, **when I** view the activity bar in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0443 — the activity bar — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the activity bar in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0444 — the activity bar — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the activity bar in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0445 — the activity bar — narrow 700px wide, reado-dark
**As a** user, **when I** view the activity bar in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0446 — the activity bar — narrow 700px wide, reado-light
**As a** user, **when I** view the activity bar in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0447 — the activity bar — narrow 700px wide, reado-sepia
**As a** user, **when I** view the activity bar in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0448 — the activity bar — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the activity bar in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0449 — the breadcrumb — 1280×832 (default), reado-dark
**As a** user, **when I** view the breadcrumb in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0450 — the breadcrumb — 1280×832 (default), reado-light
**As a** user, **when I** view the breadcrumb in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0451 — the breadcrumb — 1280×832 (default), reado-sepia
**As a** user, **when I** view the breadcrumb in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0452 — the breadcrumb — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the breadcrumb in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0453 — the breadcrumb — 1024×640 (small), reado-dark
**As a** user, **when I** view the breadcrumb in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0454 — the breadcrumb — 1024×640 (small), reado-light
**As a** user, **when I** view the breadcrumb in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0455 — the breadcrumb — 1024×640 (small), reado-sepia
**As a** user, **when I** view the breadcrumb in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0456 — the breadcrumb — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the breadcrumb in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0457 — the breadcrumb — 1920×1080 (large), reado-dark
**As a** user, **when I** view the breadcrumb in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0458 — the breadcrumb — 1920×1080 (large), reado-light
**As a** user, **when I** view the breadcrumb in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0459 — the breadcrumb — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the breadcrumb in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0460 — the breadcrumb — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the breadcrumb in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0461 — the breadcrumb — narrow 700px wide, reado-dark
**As a** user, **when I** view the breadcrumb in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0462 — the breadcrumb — narrow 700px wide, reado-light
**As a** user, **when I** view the breadcrumb in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0463 — the breadcrumb — narrow 700px wide, reado-sepia
**As a** user, **when I** view the breadcrumb in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0464 — the breadcrumb — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the breadcrumb in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0465 — the structure ribbon — 1280×832 (default), reado-dark
**As a** user, **when I** view the structure ribbon in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0466 — the structure ribbon — 1280×832 (default), reado-light
**As a** user, **when I** view the structure ribbon in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0467 — the structure ribbon — 1280×832 (default), reado-sepia
**As a** user, **when I** view the structure ribbon in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0468 — the structure ribbon — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the structure ribbon in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0469 — the structure ribbon — 1024×640 (small), reado-dark
**As a** user, **when I** view the structure ribbon in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0470 — the structure ribbon — 1024×640 (small), reado-light
**As a** user, **when I** view the structure ribbon in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0471 — the structure ribbon — 1024×640 (small), reado-sepia
**As a** user, **when I** view the structure ribbon in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0472 — the structure ribbon — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the structure ribbon in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0473 — the structure ribbon — 1920×1080 (large), reado-dark
**As a** user, **when I** view the structure ribbon in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0474 — the structure ribbon — 1920×1080 (large), reado-light
**As a** user, **when I** view the structure ribbon in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0475 — the structure ribbon — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the structure ribbon in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0476 — the structure ribbon — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the structure ribbon in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0477 — the structure ribbon — narrow 700px wide, reado-dark
**As a** user, **when I** view the structure ribbon in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0478 — the structure ribbon — narrow 700px wide, reado-light
**As a** user, **when I** view the structure ribbon in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0479 — the structure ribbon — narrow 700px wide, reado-sepia
**As a** user, **when I** view the structure ribbon in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0480 — the structure ribbon — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the structure ribbon in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0481 — the title bar — 1280×832 (default), reado-dark
**As a** user, **when I** view the title bar in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0482 — the title bar — 1280×832 (default), reado-light
**As a** user, **when I** view the title bar in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0483 — the title bar — 1280×832 (default), reado-sepia
**As a** user, **when I** view the title bar in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0484 — the title bar — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the title bar in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0485 — the title bar — 1024×640 (small), reado-dark
**As a** user, **when I** view the title bar in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0486 — the title bar — 1024×640 (small), reado-light
**As a** user, **when I** view the title bar in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0487 — the title bar — 1024×640 (small), reado-sepia
**As a** user, **when I** view the title bar in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0488 — the title bar — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the title bar in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0489 — the title bar — 1920×1080 (large), reado-dark
**As a** user, **when I** view the title bar in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0490 — the title bar — 1920×1080 (large), reado-light
**As a** user, **when I** view the title bar in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0491 — the title bar — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the title bar in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0492 — the title bar — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the title bar in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0493 — the title bar — narrow 700px wide, reado-dark
**As a** user, **when I** view the title bar in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0494 — the title bar — narrow 700px wide, reado-light
**As a** user, **when I** view the title bar in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0495 — the title bar — narrow 700px wide, reado-sepia
**As a** user, **when I** view the title bar in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0496 — the title bar — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the title bar in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0497 — the knowledge graph — 1280×832 (default), reado-dark
**As a** user, **when I** view the knowledge graph in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0498 — the knowledge graph — 1280×832 (default), reado-light
**As a** user, **when I** view the knowledge graph in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0499 — the knowledge graph — 1280×832 (default), reado-sepia
**As a** user, **when I** view the knowledge graph in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0500 — the knowledge graph — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the knowledge graph in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0501 — the knowledge graph — 1024×640 (small), reado-dark
**As a** user, **when I** view the knowledge graph in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0502 — the knowledge graph — 1024×640 (small), reado-light
**As a** user, **when I** view the knowledge graph in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0503 — the knowledge graph — 1024×640 (small), reado-sepia
**As a** user, **when I** view the knowledge graph in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0504 — the knowledge graph — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the knowledge graph in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0505 — the knowledge graph — 1920×1080 (large), reado-dark
**As a** user, **when I** view the knowledge graph in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0506 — the knowledge graph — 1920×1080 (large), reado-light
**As a** user, **when I** view the knowledge graph in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0507 — the knowledge graph — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the knowledge graph in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0508 — the knowledge graph — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the knowledge graph in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0509 — the knowledge graph — narrow 700px wide, reado-dark
**As a** user, **when I** view the knowledge graph in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0510 — the knowledge graph — narrow 700px wide, reado-light
**As a** user, **when I** view the knowledge graph in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0511 — the knowledge graph — narrow 700px wide, reado-sepia
**As a** user, **when I** view the knowledge graph in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0512 — the knowledge graph — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the knowledge graph in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0513 — the docs view — 1280×832 (default), reado-dark
**As a** user, **when I** view the docs view in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0514 — the docs view — 1280×832 (default), reado-light
**As a** user, **when I** view the docs view in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0515 — the docs view — 1280×832 (default), reado-sepia
**As a** user, **when I** view the docs view in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0516 — the docs view — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the docs view in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0517 — the docs view — 1024×640 (small), reado-dark
**As a** user, **when I** view the docs view in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0518 — the docs view — 1024×640 (small), reado-light
**As a** user, **when I** view the docs view in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0519 — the docs view — 1024×640 (small), reado-sepia
**As a** user, **when I** view the docs view in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0520 — the docs view — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the docs view in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0521 — the docs view — 1920×1080 (large), reado-dark
**As a** user, **when I** view the docs view in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0522 — the docs view — 1920×1080 (large), reado-light
**As a** user, **when I** view the docs view in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0523 — the docs view — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the docs view in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0524 — the docs view — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the docs view in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0525 — the docs view — narrow 700px wide, reado-dark
**As a** user, **when I** view the docs view in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0526 — the docs view — narrow 700px wide, reado-light
**As a** user, **when I** view the docs view in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0527 — the docs view — narrow 700px wide, reado-sepia
**As a** user, **when I** view the docs view in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0528 — the docs view — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the docs view in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0529 — a context menu — 1280×832 (default), reado-dark
**As a** user, **when I** view a context menu in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0530 — a context menu — 1280×832 (default), reado-light
**As a** user, **when I** view a context menu in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0531 — a context menu — 1280×832 (default), reado-sepia
**As a** user, **when I** view a context menu in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0532 — a context menu — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view a context menu in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0533 — a context menu — 1024×640 (small), reado-dark
**As a** user, **when I** view a context menu in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0534 — a context menu — 1024×640 (small), reado-light
**As a** user, **when I** view a context menu in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0535 — a context menu — 1024×640 (small), reado-sepia
**As a** user, **when I** view a context menu in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0536 — a context menu — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view a context menu in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0537 — a context menu — 1920×1080 (large), reado-dark
**As a** user, **when I** view a context menu in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0538 — a context menu — 1920×1080 (large), reado-light
**As a** user, **when I** view a context menu in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0539 — a context menu — 1920×1080 (large), reado-sepia
**As a** user, **when I** view a context menu in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0540 — a context menu — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view a context menu in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0541 — a context menu — narrow 700px wide, reado-dark
**As a** user, **when I** view a context menu in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0542 — a context menu — narrow 700px wide, reado-light
**As a** user, **when I** view a context menu in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0543 — a context menu — narrow 700px wide, reado-sepia
**As a** user, **when I** view a context menu in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0544 — a context menu — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view a context menu in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0545 — the update prompt — 1280×832 (default), reado-dark
**As a** user, **when I** view the update prompt in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0546 — the update prompt — 1280×832 (default), reado-light
**As a** user, **when I** view the update prompt in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0547 — the update prompt — 1280×832 (default), reado-sepia
**As a** user, **when I** view the update prompt in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0548 — the update prompt — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the update prompt in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0549 — the update prompt — 1024×640 (small), reado-dark
**As a** user, **when I** view the update prompt in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0550 — the update prompt — 1024×640 (small), reado-light
**As a** user, **when I** view the update prompt in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0551 — the update prompt — 1024×640 (small), reado-sepia
**As a** user, **when I** view the update prompt in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0552 — the update prompt — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the update prompt in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0553 — the update prompt — 1920×1080 (large), reado-dark
**As a** user, **when I** view the update prompt in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0554 — the update prompt — 1920×1080 (large), reado-light
**As a** user, **when I** view the update prompt in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0555 — the update prompt — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the update prompt in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0556 — the update prompt — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the update prompt in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0557 — the update prompt — narrow 700px wide, reado-dark
**As a** user, **when I** view the update prompt in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0558 — the update prompt — narrow 700px wide, reado-light
**As a** user, **when I** view the update prompt in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0559 — the update prompt — narrow 700px wide, reado-sepia
**As a** user, **when I** view the update prompt in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0560 — the update prompt — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the update prompt in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0561 — the welcome screen — 1280×832 (default), reado-dark
**As a** user, **when I** view the welcome screen in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0562 — the welcome screen — 1280×832 (default), reado-light
**As a** user, **when I** view the welcome screen in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0563 — the welcome screen — 1280×832 (default), reado-sepia
**As a** user, **when I** view the welcome screen in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0564 — the welcome screen — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the welcome screen in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0565 — the welcome screen — 1024×640 (small), reado-dark
**As a** user, **when I** view the welcome screen in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0566 — the welcome screen — 1024×640 (small), reado-light
**As a** user, **when I** view the welcome screen in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0567 — the welcome screen — 1024×640 (small), reado-sepia
**As a** user, **when I** view the welcome screen in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0568 — the welcome screen — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the welcome screen in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0569 — the welcome screen — 1920×1080 (large), reado-dark
**As a** user, **when I** view the welcome screen in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0570 — the welcome screen — 1920×1080 (large), reado-light
**As a** user, **when I** view the welcome screen in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0571 — the welcome screen — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the welcome screen in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0572 — the welcome screen — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the welcome screen in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0573 — the welcome screen — narrow 700px wide, reado-dark
**As a** user, **when I** view the welcome screen in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0574 — the welcome screen — narrow 700px wide, reado-light
**As a** user, **when I** view the welcome screen in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0575 — the welcome screen — narrow 700px wide, reado-sepia
**As a** user, **when I** view the welcome screen in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0576 — the welcome screen — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the welcome screen in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0577 — the orphans panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the orphans panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0578 — the orphans panel — 1280×832 (default), reado-light
**As a** user, **when I** view the orphans panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0579 — the orphans panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the orphans panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0580 — the orphans panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the orphans panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0581 — the orphans panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the orphans panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0582 — the orphans panel — 1024×640 (small), reado-light
**As a** user, **when I** view the orphans panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0583 — the orphans panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the orphans panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0584 — the orphans panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the orphans panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0585 — the orphans panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the orphans panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0586 — the orphans panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the orphans panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0587 — the orphans panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the orphans panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0588 — the orphans panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the orphans panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0589 — the orphans panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the orphans panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0590 — the orphans panel — narrow 700px wide, reado-light
**As a** user, **when I** view the orphans panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0591 — the orphans panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the orphans panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0592 — the orphans panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the orphans panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0593 — the bookmarks panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the bookmarks panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0594 — the bookmarks panel — 1280×832 (default), reado-light
**As a** user, **when I** view the bookmarks panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0595 — the bookmarks panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the bookmarks panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0596 — the bookmarks panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the bookmarks panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0597 — the bookmarks panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the bookmarks panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0598 — the bookmarks panel — 1024×640 (small), reado-light
**As a** user, **when I** view the bookmarks panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0599 — the bookmarks panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the bookmarks panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0600 — the bookmarks panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the bookmarks panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0601 — the bookmarks panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the bookmarks panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0602 — the bookmarks panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the bookmarks panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0603 — the bookmarks panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the bookmarks panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0604 — the bookmarks panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the bookmarks panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0605 — the bookmarks panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the bookmarks panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0606 — the bookmarks panel — narrow 700px wide, reado-light
**As a** user, **when I** view the bookmarks panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0607 — the bookmarks panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the bookmarks panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0608 — the bookmarks panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the bookmarks panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0609 — the timeline panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the timeline panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0610 — the timeline panel — 1280×832 (default), reado-light
**As a** user, **when I** view the timeline panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0611 — the timeline panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the timeline panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0612 — the timeline panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the timeline panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0613 — the timeline panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the timeline panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0614 — the timeline panel — 1024×640 (small), reado-light
**As a** user, **when I** view the timeline panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0615 — the timeline panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the timeline panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0616 — the timeline panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the timeline panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0617 — the timeline panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the timeline panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0618 — the timeline panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the timeline panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0619 — the timeline panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the timeline panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0620 — the timeline panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the timeline panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0621 — the timeline panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the timeline panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0622 — the timeline panel — narrow 700px wide, reado-light
**As a** user, **when I** view the timeline panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0623 — the timeline panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the timeline panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0624 — the timeline panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the timeline panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0625 — the extensions panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the extensions panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0626 — the extensions panel — 1280×832 (default), reado-light
**As a** user, **when I** view the extensions panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0627 — the extensions panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the extensions panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0628 — the extensions panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the extensions panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0629 — the extensions panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the extensions panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0630 — the extensions panel — 1024×640 (small), reado-light
**As a** user, **when I** view the extensions panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0631 — the extensions panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the extensions panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0632 — the extensions panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the extensions panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0633 — the extensions panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the extensions panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0634 — the extensions panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the extensions panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0635 — the extensions panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the extensions panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0636 — the extensions panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the extensions panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0637 — the extensions panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the extensions panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0638 — the extensions panel — narrow 700px wide, reado-light
**As a** user, **when I** view the extensions panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0639 — the extensions panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the extensions panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0640 — the extensions panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the extensions panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0641 — the specs panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the specs panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0642 — the specs panel — 1280×832 (default), reado-light
**As a** user, **when I** view the specs panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0643 — the specs panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the specs panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0644 — the specs panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the specs panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0645 — the specs panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the specs panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0646 — the specs panel — 1024×640 (small), reado-light
**As a** user, **when I** view the specs panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0647 — the specs panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the specs panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0648 — the specs panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the specs panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0649 — the specs panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the specs panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0650 — the specs panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the specs panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0651 — the specs panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the specs panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0652 — the specs panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the specs panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0653 — the specs panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the specs panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0654 — the specs panel — narrow 700px wide, reado-light
**As a** user, **when I** view the specs panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0655 — the specs panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the specs panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0656 — the specs panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the specs panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0657 — the tours panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the tours panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0658 — the tours panel — 1280×832 (default), reado-light
**As a** user, **when I** view the tours panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0659 — the tours panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the tours panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0660 — the tours panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the tours panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0661 — the tours panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the tours panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0662 — the tours panel — 1024×640 (small), reado-light
**As a** user, **when I** view the tours panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0663 — the tours panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the tours panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0664 — the tours panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the tours panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0665 — the tours panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the tours panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0666 — the tours panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the tours panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0667 — the tours panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the tours panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0668 — the tours panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the tours panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0669 — the tours panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the tours panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0670 — the tours panel — narrow 700px wide, reado-light
**As a** user, **when I** view the tours panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0671 — the tours panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the tours panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0672 — the tours panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the tours panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0673 — the pre-review panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the pre-review panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0674 — the pre-review panel — 1280×832 (default), reado-light
**As a** user, **when I** view the pre-review panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0675 — the pre-review panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the pre-review panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0676 — the pre-review panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the pre-review panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0677 — the pre-review panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the pre-review panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0678 — the pre-review panel — 1024×640 (small), reado-light
**As a** user, **when I** view the pre-review panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0679 — the pre-review panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the pre-review panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0680 — the pre-review panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the pre-review panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0681 — the pre-review panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the pre-review panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0682 — the pre-review panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the pre-review panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0683 — the pre-review panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the pre-review panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0684 — the pre-review panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the pre-review panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0685 — the pre-review panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the pre-review panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0686 — the pre-review panel — narrow 700px wide, reado-light
**As a** user, **when I** view the pre-review panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0687 — the pre-review panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the pre-review panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0688 — the pre-review panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the pre-review panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0689 — the tests panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the tests panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0690 — the tests panel — 1280×832 (default), reado-light
**As a** user, **when I** view the tests panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0691 — the tests panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the tests panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0692 — the tests panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the tests panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0693 — the tests panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the tests panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0694 — the tests panel — 1024×640 (small), reado-light
**As a** user, **when I** view the tests panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0695 — the tests panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the tests panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0696 — the tests panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the tests panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0697 — the tests panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the tests panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0698 — the tests panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the tests panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0699 — the tests panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the tests panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0700 — the tests panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the tests panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0701 — the tests panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the tests panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0702 — the tests panel — narrow 700px wide, reado-light
**As a** user, **when I** view the tests panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0703 — the tests panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the tests panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0704 — the tests panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the tests panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0705 — the QA panel — 1280×832 (default), reado-dark
**As a** user, **when I** view the QA panel in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0706 — the QA panel — 1280×832 (default), reado-light
**As a** user, **when I** view the QA panel in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0707 — the QA panel — 1280×832 (default), reado-sepia
**As a** user, **when I** view the QA panel in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0708 — the QA panel — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the QA panel in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0709 — the QA panel — 1024×640 (small), reado-dark
**As a** user, **when I** view the QA panel in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0710 — the QA panel — 1024×640 (small), reado-light
**As a** user, **when I** view the QA panel in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0711 — the QA panel — 1024×640 (small), reado-sepia
**As a** user, **when I** view the QA panel in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0712 — the QA panel — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the QA panel in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0713 — the QA panel — 1920×1080 (large), reado-dark
**As a** user, **when I** view the QA panel in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0714 — the QA panel — 1920×1080 (large), reado-light
**As a** user, **when I** view the QA panel in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0715 — the QA panel — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the QA panel in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0716 — the QA panel — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the QA panel in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0717 — the QA panel — narrow 700px wide, reado-dark
**As a** user, **when I** view the QA panel in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0718 — the QA panel — narrow 700px wide, reado-light
**As a** user, **when I** view the QA panel in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0719 — the QA panel — narrow 700px wide, reado-sepia
**As a** user, **when I** view the QA panel in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0720 — the QA panel — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the QA panel in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0721 — the send-review dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the send-review dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0722 — the send-review dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the send-review dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0723 — the send-review dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the send-review dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0724 — the send-review dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the send-review dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0725 — the send-review dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the send-review dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0726 — the send-review dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the send-review dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0727 — the send-review dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the send-review dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0728 — the send-review dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the send-review dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0729 — the send-review dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the send-review dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0730 — the send-review dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the send-review dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0731 — the send-review dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the send-review dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0732 — the send-review dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the send-review dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0733 — the send-review dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the send-review dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0734 — the send-review dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the send-review dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0735 — the send-review dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the send-review dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0736 — the send-review dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the send-review dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0737 — the tree-comment dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the tree-comment dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0738 — the tree-comment dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the tree-comment dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0739 — the tree-comment dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the tree-comment dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0740 — the tree-comment dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the tree-comment dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0741 — the tree-comment dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the tree-comment dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0742 — the tree-comment dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the tree-comment dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0743 — the tree-comment dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the tree-comment dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0744 — the tree-comment dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the tree-comment dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0745 — the tree-comment dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the tree-comment dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0746 — the tree-comment dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the tree-comment dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0747 — the tree-comment dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the tree-comment dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0748 — the tree-comment dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the tree-comment dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0749 — the tree-comment dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the tree-comment dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0750 — the tree-comment dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the tree-comment dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0751 — the tree-comment dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the tree-comment dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0752 — the tree-comment dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the tree-comment dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0753 — the prompt dialog — 1280×832 (default), reado-dark
**As a** user, **when I** view the prompt dialog in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0754 — the prompt dialog — 1280×832 (default), reado-light
**As a** user, **when I** view the prompt dialog in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0755 — the prompt dialog — 1280×832 (default), reado-sepia
**As a** user, **when I** view the prompt dialog in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0756 — the prompt dialog — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the prompt dialog in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0757 — the prompt dialog — 1024×640 (small), reado-dark
**As a** user, **when I** view the prompt dialog in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0758 — the prompt dialog — 1024×640 (small), reado-light
**As a** user, **when I** view the prompt dialog in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0759 — the prompt dialog — 1024×640 (small), reado-sepia
**As a** user, **when I** view the prompt dialog in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0760 — the prompt dialog — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the prompt dialog in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0761 — the prompt dialog — 1920×1080 (large), reado-dark
**As a** user, **when I** view the prompt dialog in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0762 — the prompt dialog — 1920×1080 (large), reado-light
**As a** user, **when I** view the prompt dialog in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0763 — the prompt dialog — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the prompt dialog in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0764 — the prompt dialog — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the prompt dialog in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0765 — the prompt dialog — narrow 700px wide, reado-dark
**As a** user, **when I** view the prompt dialog in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0766 — the prompt dialog — narrow 700px wide, reado-light
**As a** user, **when I** view the prompt dialog in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0767 — the prompt dialog — narrow 700px wide, reado-sepia
**As a** user, **when I** view the prompt dialog in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0768 — the prompt dialog — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the prompt dialog in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0769 — the synopsis modal — 1280×832 (default), reado-dark
**As a** user, **when I** view the synopsis modal in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0770 — the synopsis modal — 1280×832 (default), reado-light
**As a** user, **when I** view the synopsis modal in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0771 — the synopsis modal — 1280×832 (default), reado-sepia
**As a** user, **when I** view the synopsis modal in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0772 — the synopsis modal — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the synopsis modal in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0773 — the synopsis modal — 1024×640 (small), reado-dark
**As a** user, **when I** view the synopsis modal in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0774 — the synopsis modal — 1024×640 (small), reado-light
**As a** user, **when I** view the synopsis modal in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0775 — the synopsis modal — 1024×640 (small), reado-sepia
**As a** user, **when I** view the synopsis modal in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0776 — the synopsis modal — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the synopsis modal in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0777 — the synopsis modal — 1920×1080 (large), reado-dark
**As a** user, **when I** view the synopsis modal in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0778 — the synopsis modal — 1920×1080 (large), reado-light
**As a** user, **when I** view the synopsis modal in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0779 — the synopsis modal — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the synopsis modal in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0780 — the synopsis modal — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the synopsis modal in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0781 — the synopsis modal — narrow 700px wide, reado-dark
**As a** user, **when I** view the synopsis modal in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0782 — the synopsis modal — narrow 700px wide, reado-light
**As a** user, **when I** view the synopsis modal in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0783 — the synopsis modal — narrow 700px wide, reado-sepia
**As a** user, **when I** view the synopsis modal in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0784 — the synopsis modal — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the synopsis modal in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0785 — the gitignore prompt — 1280×832 (default), reado-dark
**As a** user, **when I** view the gitignore prompt in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0786 — the gitignore prompt — 1280×832 (default), reado-light
**As a** user, **when I** view the gitignore prompt in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0787 — the gitignore prompt — 1280×832 (default), reado-sepia
**As a** user, **when I** view the gitignore prompt in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0788 — the gitignore prompt — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the gitignore prompt in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0789 — the gitignore prompt — 1024×640 (small), reado-dark
**As a** user, **when I** view the gitignore prompt in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0790 — the gitignore prompt — 1024×640 (small), reado-light
**As a** user, **when I** view the gitignore prompt in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0791 — the gitignore prompt — 1024×640 (small), reado-sepia
**As a** user, **when I** view the gitignore prompt in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0792 — the gitignore prompt — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the gitignore prompt in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0793 — the gitignore prompt — 1920×1080 (large), reado-dark
**As a** user, **when I** view the gitignore prompt in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0794 — the gitignore prompt — 1920×1080 (large), reado-light
**As a** user, **when I** view the gitignore prompt in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0795 — the gitignore prompt — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the gitignore prompt in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0796 — the gitignore prompt — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the gitignore prompt in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0797 — the gitignore prompt — narrow 700px wide, reado-dark
**As a** user, **when I** view the gitignore prompt in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0798 — the gitignore prompt — narrow 700px wide, reado-light
**As a** user, **when I** view the gitignore prompt in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0799 — the gitignore prompt — narrow 700px wide, reado-sepia
**As a** user, **when I** view the gitignore prompt in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0800 — the gitignore prompt — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the gitignore prompt in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0801 — the resolve-loop bar — 1280×832 (default), reado-dark
**As a** user, **when I** view the resolve-loop bar in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0802 — the resolve-loop bar — 1280×832 (default), reado-light
**As a** user, **when I** view the resolve-loop bar in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0803 — the resolve-loop bar — 1280×832 (default), reado-sepia
**As a** user, **when I** view the resolve-loop bar in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0804 — the resolve-loop bar — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the resolve-loop bar in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0805 — the resolve-loop bar — 1024×640 (small), reado-dark
**As a** user, **when I** view the resolve-loop bar in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0806 — the resolve-loop bar — 1024×640 (small), reado-light
**As a** user, **when I** view the resolve-loop bar in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0807 — the resolve-loop bar — 1024×640 (small), reado-sepia
**As a** user, **when I** view the resolve-loop bar in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0808 — the resolve-loop bar — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the resolve-loop bar in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0809 — the resolve-loop bar — 1920×1080 (large), reado-dark
**As a** user, **when I** view the resolve-loop bar in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0810 — the resolve-loop bar — 1920×1080 (large), reado-light
**As a** user, **when I** view the resolve-loop bar in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0811 — the resolve-loop bar — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the resolve-loop bar in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0812 — the resolve-loop bar — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the resolve-loop bar in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0813 — the resolve-loop bar — narrow 700px wide, reado-dark
**As a** user, **when I** view the resolve-loop bar in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0814 — the resolve-loop bar — narrow 700px wide, reado-light
**As a** user, **when I** view the resolve-loop bar in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0815 — the resolve-loop bar — narrow 700px wide, reado-sepia
**As a** user, **when I** view the resolve-loop bar in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0816 — the resolve-loop bar — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the resolve-loop bar in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0817 — the image viewer — 1280×832 (default), reado-dark
**As a** user, **when I** view the image viewer in a 1280×832 (default) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0818 — the image viewer — 1280×832 (default), reado-light
**As a** user, **when I** view the image viewer in a 1280×832 (default) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0819 — the image viewer — 1280×832 (default), reado-sepia
**As a** user, **when I** view the image viewer in a 1280×832 (default) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0820 — the image viewer — 1280×832 (default), reado-high-contrast
**As a** user, **when I** view the image viewer in a 1280×832 (default) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0821 — the image viewer — 1024×640 (small), reado-dark
**As a** user, **when I** view the image viewer in a 1024×640 (small) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0822 — the image viewer — 1024×640 (small), reado-light
**As a** user, **when I** view the image viewer in a 1024×640 (small) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0823 — the image viewer — 1024×640 (small), reado-sepia
**As a** user, **when I** view the image viewer in a 1024×640 (small) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0824 — the image viewer — 1024×640 (small), reado-high-contrast
**As a** user, **when I** view the image viewer in a 1024×640 (small) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0825 — the image viewer — 1920×1080 (large), reado-dark
**As a** user, **when I** view the image viewer in a 1920×1080 (large) window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0826 — the image viewer — 1920×1080 (large), reado-light
**As a** user, **when I** view the image viewer in a 1920×1080 (large) window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0827 — the image viewer — 1920×1080 (large), reado-sepia
**As a** user, **when I** view the image viewer in a 1920×1080 (large) window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0828 — the image viewer — 1920×1080 (large), reado-high-contrast
**As a** user, **when I** view the image viewer in a 1920×1080 (large) window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0829 — the image viewer — narrow 700px wide, reado-dark
**As a** user, **when I** view the image viewer in a narrow 700px wide window with the reado-dark theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0830 — the image viewer — narrow 700px wide, reado-light
**As a** user, **when I** view the image viewer in a narrow 700px wide window with the reado-light theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0831 — the image viewer — narrow 700px wide, reado-sepia
**As a** user, **when I** view the image viewer in a narrow 700px wide window with the reado-sepia theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO

### TC-RESP-0832 — the image viewer — narrow 700px wide, reado-high-contrast
**As a** user, **when I** view the image viewer in a narrow 700px wide window with the reado-high-contrast theme, **I expect** the layout reflows sensibly; nothing critical clipped or unreachable.
- **Result:** TODO
