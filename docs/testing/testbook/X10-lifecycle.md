# X10 — Lifecycle (start / reload / multi-window)

Cold start, first run, multi-window, reload and project-switch — the lifecycle paths that surfaced BUG-1 and BUG-2.

**Cases: 208.**

---

### TC-LIFE-0001 — the launcher on a cold start into the last project
**As a** user, **when I** observe the launcher during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0002 — the launcher on a true first run (no settings/recents)
**As a** user, **when I** observe the launcher during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0003 — the launcher on a second window on the same project
**As a** user, **when I** observe the launcher during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0004 — the launcher on a webview reload mid-session
**As a** user, **when I** observe the launcher during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0005 — the file tree on a cold start into the last project
**As a** user, **when I** observe the file tree during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0006 — the file tree on a true first run (no settings/recents)
**As a** user, **when I** observe the file tree during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0007 — the file tree on a second window on the same project
**As a** user, **when I** observe the file tree during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0008 — the file tree on a webview reload mid-session
**As a** user, **when I** observe the file tree during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0009 — the editor on a cold start into the last project
**As a** user, **when I** observe the editor during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0010 — the editor on a true first run (no settings/recents)
**As a** user, **when I** observe the editor during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0011 — the editor on a second window on the same project
**As a** user, **when I** observe the editor during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0012 — the editor on a webview reload mid-session
**As a** user, **when I** observe the editor during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0013 — the tab bar on a cold start into the last project
**As a** user, **when I** observe the tab bar during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0014 — the tab bar on a true first run (no settings/recents)
**As a** user, **when I** observe the tab bar during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0015 — the tab bar on a second window on the same project
**As a** user, **when I** observe the tab bar during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0016 — the tab bar on a webview reload mid-session
**As a** user, **when I** observe the tab bar during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0017 — the diff view on a cold start into the last project
**As a** user, **when I** observe the diff view during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0018 — the diff view on a true first run (no settings/recents)
**As a** user, **when I** observe the diff view during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0019 — the diff view on a second window on the same project
**As a** user, **when I** observe the diff view during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0020 — the diff view on a webview reload mid-session
**As a** user, **when I** observe the diff view during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0021 — the blame gutter on a cold start into the last project
**As a** user, **when I** observe the blame gutter during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0022 — the blame gutter on a true first run (no settings/recents)
**As a** user, **when I** observe the blame gutter during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0023 — the blame gutter on a second window on the same project
**As a** user, **when I** observe the blame gutter during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0024 — the blame gutter on a webview reload mid-session
**As a** user, **when I** observe the blame gutter during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0025 — the search panel on a cold start into the last project
**As a** user, **when I** observe the search panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0026 — the search panel on a true first run (no settings/recents)
**As a** user, **when I** observe the search panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0027 — the search panel on a second window on the same project
**As a** user, **when I** observe the search panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0028 — the search panel on a webview reload mid-session
**As a** user, **when I** observe the search panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0029 — the comments panel on a cold start into the last project
**As a** user, **when I** observe the comments panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0030 — the comments panel on a true first run (no settings/recents)
**As a** user, **when I** observe the comments panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0031 — the comments panel on a second window on the same project
**As a** user, **when I** observe the comments panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0032 — the comments panel on a webview reload mid-session
**As a** user, **when I** observe the comments panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0033 — a comment thread on a cold start into the last project
**As a** user, **when I** observe a comment thread during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0034 — a comment thread on a true first run (no settings/recents)
**As a** user, **when I** observe a comment thread during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0035 — a comment thread on a second window on the same project
**As a** user, **when I** observe a comment thread during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0036 — a comment thread on a webview reload mid-session
**As a** user, **when I** observe a comment thread during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0037 — the comment composer on a cold start into the last project
**As a** user, **when I** observe the comment composer during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0038 — the comment composer on a true first run (no settings/recents)
**As a** user, **when I** observe the comment composer during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0039 — the comment composer on a second window on the same project
**As a** user, **when I** observe the comment composer during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0040 — the comment composer on a webview reload mid-session
**As a** user, **when I** observe the comment composer during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0041 — the git panel on a cold start into the last project
**As a** user, **when I** observe the git panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0042 — the git panel on a true first run (no settings/recents)
**As a** user, **when I** observe the git panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0043 — the git panel on a second window on the same project
**As a** user, **when I** observe the git panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0044 — the git panel on a webview reload mid-session
**As a** user, **when I** observe the git panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0045 — the branch switcher on a cold start into the last project
**As a** user, **when I** observe the branch switcher during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0046 — the branch switcher on a true first run (no settings/recents)
**As a** user, **when I** observe the branch switcher during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0047 — the branch switcher on a second window on the same project
**As a** user, **when I** observe the branch switcher during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0048 — the branch switcher on a webview reload mid-session
**As a** user, **when I** observe the branch switcher during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0049 — the guided-review panel on a cold start into the last project
**As a** user, **when I** observe the guided-review panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0050 — the guided-review panel on a true first run (no settings/recents)
**As a** user, **when I** observe the guided-review panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0051 — the guided-review panel on a second window on the same project
**As a** user, **when I** observe the guided-review panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0052 — the guided-review panel on a webview reload mid-session
**As a** user, **when I** observe the guided-review panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0053 — the terminal on a cold start into the last project
**As a** user, **when I** observe the terminal during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0054 — the terminal on a true first run (no settings/recents)
**As a** user, **when I** observe the terminal during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0055 — the terminal on a second window on the same project
**As a** user, **when I** observe the terminal during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0056 — the terminal on a webview reload mid-session
**As a** user, **when I** observe the terminal during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0057 — the outline panel on a cold start into the last project
**As a** user, **when I** observe the outline panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0058 — the outline panel on a true first run (no settings/recents)
**As a** user, **when I** observe the outline panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0059 — the outline panel on a second window on the same project
**As a** user, **when I** observe the outline panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0060 — the outline panel on a webview reload mid-session
**As a** user, **when I** observe the outline panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0061 — the problems panel on a cold start into the last project
**As a** user, **when I** observe the problems panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0062 — the problems panel on a true first run (no settings/recents)
**As a** user, **when I** observe the problems panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0063 — the problems panel on a second window on the same project
**As a** user, **when I** observe the problems panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0064 — the problems panel on a webview reload mid-session
**As a** user, **when I** observe the problems panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0065 — the hierarchy panel on a cold start into the last project
**As a** user, **when I** observe the hierarchy panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0066 — the hierarchy panel on a true first run (no settings/recents)
**As a** user, **when I** observe the hierarchy panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0067 — the hierarchy panel on a second window on the same project
**As a** user, **when I** observe the hierarchy panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0068 — the hierarchy panel on a webview reload mid-session
**As a** user, **when I** observe the hierarchy panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0069 — the command palette on a cold start into the last project
**As a** user, **when I** observe the command palette during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0070 — the command palette on a true first run (no settings/recents)
**As a** user, **when I** observe the command palette during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0071 — the command palette on a second window on the same project
**As a** user, **when I** observe the command palette during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0072 — the command palette on a webview reload mid-session
**As a** user, **when I** observe the command palette during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0073 — the file finder on a cold start into the last project
**As a** user, **when I** observe the file finder during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0074 — the file finder on a true first run (no settings/recents)
**As a** user, **when I** observe the file finder during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0075 — the file finder on a second window on the same project
**As a** user, **when I** observe the file finder during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0076 — the file finder on a webview reload mid-session
**As a** user, **when I** observe the file finder during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0077 — the settings dialog on a cold start into the last project
**As a** user, **when I** observe the settings dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0078 — the settings dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the settings dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0079 — the settings dialog on a second window on the same project
**As a** user, **when I** observe the settings dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0080 — the settings dialog on a webview reload mid-session
**As a** user, **when I** observe the settings dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0081 — the shortcuts dialog on a cold start into the last project
**As a** user, **when I** observe the shortcuts dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0082 — the shortcuts dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the shortcuts dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0083 — the shortcuts dialog on a second window on the same project
**As a** user, **when I** observe the shortcuts dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0084 — the shortcuts dialog on a webview reload mid-session
**As a** user, **when I** observe the shortcuts dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0085 — the anywhere dialog on a cold start into the last project
**As a** user, **when I** observe the anywhere dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0086 — the anywhere dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the anywhere dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0087 — the anywhere dialog on a second window on the same project
**As a** user, **when I** observe the anywhere dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0088 — the anywhere dialog on a webview reload mid-session
**As a** user, **when I** observe the anywhere dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0089 — the onboarding modal on a cold start into the last project
**As a** user, **when I** observe the onboarding modal during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0090 — the onboarding modal on a true first run (no settings/recents)
**As a** user, **when I** observe the onboarding modal during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0091 — the onboarding modal on a second window on the same project
**As a** user, **when I** observe the onboarding modal during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0092 — the onboarding modal on a webview reload mid-session
**As a** user, **when I** observe the onboarding modal during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0093 — the Q&A modal on a cold start into the last project
**As a** user, **when I** observe the Q&A modal during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0094 — the Q&A modal on a true first run (no settings/recents)
**As a** user, **when I** observe the Q&A modal during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0095 — the Q&A modal on a second window on the same project
**As a** user, **when I** observe the Q&A modal during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0096 — the Q&A modal on a webview reload mid-session
**As a** user, **when I** observe the Q&A modal during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0097 — the semantic modal on a cold start into the last project
**As a** user, **when I** observe the semantic modal during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0098 — the semantic modal on a true first run (no settings/recents)
**As a** user, **when I** observe the semantic modal during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0099 — the semantic modal on a second window on the same project
**As a** user, **when I** observe the semantic modal during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0100 — the semantic modal on a webview reload mid-session
**As a** user, **when I** observe the semantic modal during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0101 — the audit dialog on a cold start into the last project
**As a** user, **when I** observe the audit dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0102 — the audit dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the audit dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0103 — the audit dialog on a second window on the same project
**As a** user, **when I** observe the audit dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0104 — the audit dialog on a webview reload mid-session
**As a** user, **when I** observe the audit dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0105 — the status bar on a cold start into the last project
**As a** user, **when I** observe the status bar during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0106 — the status bar on a true first run (no settings/recents)
**As a** user, **when I** observe the status bar during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0107 — the status bar on a second window on the same project
**As a** user, **when I** observe the status bar during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0108 — the status bar on a webview reload mid-session
**As a** user, **when I** observe the status bar during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0109 — the activity bar on a cold start into the last project
**As a** user, **when I** observe the activity bar during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0110 — the activity bar on a true first run (no settings/recents)
**As a** user, **when I** observe the activity bar during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0111 — the activity bar on a second window on the same project
**As a** user, **when I** observe the activity bar during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0112 — the activity bar on a webview reload mid-session
**As a** user, **when I** observe the activity bar during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0113 — the breadcrumb on a cold start into the last project
**As a** user, **when I** observe the breadcrumb during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0114 — the breadcrumb on a true first run (no settings/recents)
**As a** user, **when I** observe the breadcrumb during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0115 — the breadcrumb on a second window on the same project
**As a** user, **when I** observe the breadcrumb during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0116 — the breadcrumb on a webview reload mid-session
**As a** user, **when I** observe the breadcrumb during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0117 — the structure ribbon on a cold start into the last project
**As a** user, **when I** observe the structure ribbon during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0118 — the structure ribbon on a true first run (no settings/recents)
**As a** user, **when I** observe the structure ribbon during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0119 — the structure ribbon on a second window on the same project
**As a** user, **when I** observe the structure ribbon during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0120 — the structure ribbon on a webview reload mid-session
**As a** user, **when I** observe the structure ribbon during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0121 — the title bar on a cold start into the last project
**As a** user, **when I** observe the title bar during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0122 — the title bar on a true first run (no settings/recents)
**As a** user, **when I** observe the title bar during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0123 — the title bar on a second window on the same project
**As a** user, **when I** observe the title bar during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0124 — the title bar on a webview reload mid-session
**As a** user, **when I** observe the title bar during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0125 — the knowledge graph on a cold start into the last project
**As a** user, **when I** observe the knowledge graph during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0126 — the knowledge graph on a true first run (no settings/recents)
**As a** user, **when I** observe the knowledge graph during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0127 — the knowledge graph on a second window on the same project
**As a** user, **when I** observe the knowledge graph during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0128 — the knowledge graph on a webview reload mid-session
**As a** user, **when I** observe the knowledge graph during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0129 — the docs view on a cold start into the last project
**As a** user, **when I** observe the docs view during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0130 — the docs view on a true first run (no settings/recents)
**As a** user, **when I** observe the docs view during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0131 — the docs view on a second window on the same project
**As a** user, **when I** observe the docs view during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0132 — the docs view on a webview reload mid-session
**As a** user, **when I** observe the docs view during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0133 — a context menu on a cold start into the last project
**As a** user, **when I** observe a context menu during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0134 — a context menu on a true first run (no settings/recents)
**As a** user, **when I** observe a context menu during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0135 — a context menu on a second window on the same project
**As a** user, **when I** observe a context menu during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0136 — a context menu on a webview reload mid-session
**As a** user, **when I** observe a context menu during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0137 — the update prompt on a cold start into the last project
**As a** user, **when I** observe the update prompt during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0138 — the update prompt on a true first run (no settings/recents)
**As a** user, **when I** observe the update prompt during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0139 — the update prompt on a second window on the same project
**As a** user, **when I** observe the update prompt during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0140 — the update prompt on a webview reload mid-session
**As a** user, **when I** observe the update prompt during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0141 — the welcome screen on a cold start into the last project
**As a** user, **when I** observe the welcome screen during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0142 — the welcome screen on a true first run (no settings/recents)
**As a** user, **when I** observe the welcome screen during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0143 — the welcome screen on a second window on the same project
**As a** user, **when I** observe the welcome screen during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0144 — the welcome screen on a webview reload mid-session
**As a** user, **when I** observe the welcome screen during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0145 — the orphans panel on a cold start into the last project
**As a** user, **when I** observe the orphans panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0146 — the orphans panel on a true first run (no settings/recents)
**As a** user, **when I** observe the orphans panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0147 — the orphans panel on a second window on the same project
**As a** user, **when I** observe the orphans panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0148 — the orphans panel on a webview reload mid-session
**As a** user, **when I** observe the orphans panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0149 — the bookmarks panel on a cold start into the last project
**As a** user, **when I** observe the bookmarks panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0150 — the bookmarks panel on a true first run (no settings/recents)
**As a** user, **when I** observe the bookmarks panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0151 — the bookmarks panel on a second window on the same project
**As a** user, **when I** observe the bookmarks panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0152 — the bookmarks panel on a webview reload mid-session
**As a** user, **when I** observe the bookmarks panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0153 — the timeline panel on a cold start into the last project
**As a** user, **when I** observe the timeline panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0154 — the timeline panel on a true first run (no settings/recents)
**As a** user, **when I** observe the timeline panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0155 — the timeline panel on a second window on the same project
**As a** user, **when I** observe the timeline panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0156 — the timeline panel on a webview reload mid-session
**As a** user, **when I** observe the timeline panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0157 — the extensions panel on a cold start into the last project
**As a** user, **when I** observe the extensions panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0158 — the extensions panel on a true first run (no settings/recents)
**As a** user, **when I** observe the extensions panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0159 — the extensions panel on a second window on the same project
**As a** user, **when I** observe the extensions panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0160 — the extensions panel on a webview reload mid-session
**As a** user, **when I** observe the extensions panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0161 — the specs panel on a cold start into the last project
**As a** user, **when I** observe the specs panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0162 — the specs panel on a true first run (no settings/recents)
**As a** user, **when I** observe the specs panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0163 — the specs panel on a second window on the same project
**As a** user, **when I** observe the specs panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0164 — the specs panel on a webview reload mid-session
**As a** user, **when I** observe the specs panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0165 — the tours panel on a cold start into the last project
**As a** user, **when I** observe the tours panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0166 — the tours panel on a true first run (no settings/recents)
**As a** user, **when I** observe the tours panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0167 — the tours panel on a second window on the same project
**As a** user, **when I** observe the tours panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0168 — the tours panel on a webview reload mid-session
**As a** user, **when I** observe the tours panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0169 — the pre-review panel on a cold start into the last project
**As a** user, **when I** observe the pre-review panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0170 — the pre-review panel on a true first run (no settings/recents)
**As a** user, **when I** observe the pre-review panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0171 — the pre-review panel on a second window on the same project
**As a** user, **when I** observe the pre-review panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0172 — the pre-review panel on a webview reload mid-session
**As a** user, **when I** observe the pre-review panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0173 — the tests panel on a cold start into the last project
**As a** user, **when I** observe the tests panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0174 — the tests panel on a true first run (no settings/recents)
**As a** user, **when I** observe the tests panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0175 — the tests panel on a second window on the same project
**As a** user, **when I** observe the tests panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0176 — the tests panel on a webview reload mid-session
**As a** user, **when I** observe the tests panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0177 — the QA panel on a cold start into the last project
**As a** user, **when I** observe the QA panel during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0178 — the QA panel on a true first run (no settings/recents)
**As a** user, **when I** observe the QA panel during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0179 — the QA panel on a second window on the same project
**As a** user, **when I** observe the QA panel during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0180 — the QA panel on a webview reload mid-session
**As a** user, **when I** observe the QA panel during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0181 — the send-review dialog on a cold start into the last project
**As a** user, **when I** observe the send-review dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0182 — the send-review dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the send-review dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0183 — the send-review dialog on a second window on the same project
**As a** user, **when I** observe the send-review dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0184 — the send-review dialog on a webview reload mid-session
**As a** user, **when I** observe the send-review dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0185 — the tree-comment dialog on a cold start into the last project
**As a** user, **when I** observe the tree-comment dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0186 — the tree-comment dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the tree-comment dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0187 — the tree-comment dialog on a second window on the same project
**As a** user, **when I** observe the tree-comment dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0188 — the tree-comment dialog on a webview reload mid-session
**As a** user, **when I** observe the tree-comment dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0189 — the prompt dialog on a cold start into the last project
**As a** user, **when I** observe the prompt dialog during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0190 — the prompt dialog on a true first run (no settings/recents)
**As a** user, **when I** observe the prompt dialog during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0191 — the prompt dialog on a second window on the same project
**As a** user, **when I** observe the prompt dialog during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0192 — the prompt dialog on a webview reload mid-session
**As a** user, **when I** observe the prompt dialog during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0193 — the synopsis modal on a cold start into the last project
**As a** user, **when I** observe the synopsis modal during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0194 — the synopsis modal on a true first run (no settings/recents)
**As a** user, **when I** observe the synopsis modal during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0195 — the synopsis modal on a second window on the same project
**As a** user, **when I** observe the synopsis modal during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0196 — the synopsis modal on a webview reload mid-session
**As a** user, **when I** observe the synopsis modal during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0197 — the gitignore prompt on a cold start into the last project
**As a** user, **when I** observe the gitignore prompt during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0198 — the gitignore prompt on a true first run (no settings/recents)
**As a** user, **when I** observe the gitignore prompt during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0199 — the gitignore prompt on a second window on the same project
**As a** user, **when I** observe the gitignore prompt during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0200 — the gitignore prompt on a webview reload mid-session
**As a** user, **when I** observe the gitignore prompt during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0201 — the resolve-loop bar on a cold start into the last project
**As a** user, **when I** observe the resolve-loop bar during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0202 — the resolve-loop bar on a true first run (no settings/recents)
**As a** user, **when I** observe the resolve-loop bar during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0203 — the resolve-loop bar on a second window on the same project
**As a** user, **when I** observe the resolve-loop bar during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0204 — the resolve-loop bar on a webview reload mid-session
**As a** user, **when I** observe the resolve-loop bar during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0205 — the image viewer on a cold start into the last project
**As a** user, **when I** observe the image viewer during a cold start into the last project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0206 — the image viewer on a true first run (no settings/recents)
**As a** user, **when I** observe the image viewer during a true first run (no settings/recents), **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0207 — the image viewer on a second window on the same project
**As a** user, **when I** observe the image viewer during a second window on the same project, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO

### TC-LIFE-0208 — the image viewer on a webview reload mid-session
**As a** user, **when I** observe the image viewer during a webview reload mid-session, **I expect** it mounts/persists/cleans up correctly with no errors or leaks (cf. BUG-1, BUG-2).
- **Result:** TODO
